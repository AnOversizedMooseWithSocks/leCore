# Typed decisions, the swarm contract, and the NOOA discipline — syntax reference

The Jev-shaped decision surface (sweeps 171–176) and the NOOA properties carried onto it. Every JSON block
below was GENERATED by calling the live engine (`tools/gen_typed_examples.py`), so the shapes cannot drift
from the code; regenerate it after any change to these doors. Measurements behind each door are in
`docs/research/` (start at `RESEARCH_00_INDEX.md`; the benchmark tables in `BENCHMARK_sweep176.md`).

## 0. Two ways to call every door

In process:

    import lecore
    mind = lecore.agent_boot()                       # or UnifiedMind(dim=256, seed=0) with no memory
    mind.systemone_decide(state, questions, scorer="nb")

Over HTTP (the service auto-introspects every public mind method; every call below is one `POST /invoke`):

    curl -X POST http://127.0.0.1:8080/invoke -H 'Content-Type: application/json' \
         -d '{"name": "systemone_decide", "args": {"state": "...", "questions": {...}, "scorer": "nb"}}'

The reply is `{"result": ...}`; a refused call carries `"error"`. Two sandbox rules learned the hard way: the
service loads the code it was started with (restart it after an edit, or a new verb "does not exist"), and a
script run from another directory imports whatever `lecore` is first on `sys.path` (`PYTHONPATH=.` when a
PyPI leos-core is also installed).

## 1. The schema (`questions`)

Three question types. `choice`: `options` (>= 2 unique strings) with `examples` per option (a non-empty list;
keys a subset of options). `noul`: yes/no with `examples` keyed `yes` / `no`. `score`: numeric `min` < `max`
with `anchors` as **(text, value) pairs** — a value-keyed dict is refused by `validate_questions`.

```json
{
  "cat": {
    "type": "choice",
    "options": [
      "billing",
      "shipping"
    ],
    "examples": {
      "billing": [
        "card charged twice",
        "refund my invoice fee",
        "charge on my statement"
      ],
      "shipping": [
        "parcel lost in transit",
        "courier delivery late",
        "package never arrived"
      ]
    }
  },
  "priority": {
    "type": "score",
    "min": 1,
    "max": 5,
    "anchors": [
      [
        "no rush",
        1
      ],
      [
        "whenever you can",
        1.5
      ],
      [
        "needs attention this week",
        3
      ],
      [
        "urgent",
        5
      ],
      [
        "right now",
        5
      ]
    ]
  },
  "escalate": {
    "type": "noul",
    "examples": {
      "yes": [
        "angry customer",
        "threatening to cancel"
      ],
      "no": [
        "just a question",
        "curious"
      ]
    }
  }
}
```


Lint it BEFORE asking. `systemone_lint(questions, states, scorer)` reports (never rewrites): example token
budgets imbalanced > 2x (the shortest option owns the smoothing floor — measured 1–3 of 8 tool decisions until
balanced, then 8 of 8), options under 3 examples, the scorer the measured regime table recommends from k
(prototype under ~10 per class, transformed NB from ~20; never leave-one-out), states carrying more than one
clause (returned split — one observation per state), and contrastive / negated states to escalate.

### systemone_lint -> findings
```json
{
  "ok": false,
  "findings": [
    {
      "level": "error",
      "where": "escalate",
      "what": "fewer than 2 options"
    },
    {
      "level": "note",
      "where": "scorer",
      "what": "nb requested; regime table (k_min=3) recommends prototype"
    },
    {
      "level": "note",
      "where": "state[0]",
      "what": "2 clauses -- split into one state each",
      "clauses": [
        "my card was charged twice.",
        "also the parcel is late but not lost"
      ]
    },
    {
      "level": "note",
      "where": "state[0]",
      "what": "contrastive/negated -- escalate to the model end"
    }
  ],
  "recommended_scorer": "prototype",
  "k_min": 3,
  "budgets": {
    "cat": {
      "billing": {
        "examples": 3,
        "tokens": 11
      },
      "shipping": {
        "examples": 3,
        "tokens": 10
      }
    },
    "priority": {},
    "escalate": {}
  }
}
```


## 2. Deciding: `systemone_decide` / `systemone_map` / `systemone_stream`

`systemone_decide(state, questions, labeled=None, margin=None, encoder="perceive", min_support=None,
scorer="prototype"|"nb", nb_bigrams=False, conformal_alpha=None, reflex=False, escalate=None, p_floor=0.5,
verify=False)`. Use `scorer="nb"` (the transformed count table) from ~20 examples per option; `encoder="ngram"`
for text. `labeled=[[state, {question: truth}], ...]` calibrates `p` (isotonic; needs ~150 rows to be honest)
and, with `conformal_alpha`, the answer `set` — the options guaranteed to hold the truth with probability
1 − alpha (Banking77: nominal 0.95 → 0.960 measured). Every answer carries an `id`.

### systemone_decide -> answers (one per question; note id, ranked, margin_gap, p, set)
```json
{
  "cat": {
    "type": "choice",
    "confident": true,
    "abstained": false,
    "ranked": [
      [
        "shipping",
        0.6624419681041313
      ],
      [
        "billing",
        0.33755803189586875
      ]
    ],
    "margin_gap": 0.3248839362082625,
    "p": 0.875,
    "calibrated": true,
    "basis": "examples",
    "value": "shipping",
    "support": 0.6624419681041313,
    "set": [
      "shipping"
    ],
    "set_alpha": 0.1,
    "id": "2c8bf88bf5dae22a"
  },
  "priority": {
    "type": "score",
    "value": null,
    "abstained": true,
    "support": 0.011758909122849773,
    "n_anchors": 5,
    "why": "best anchor cosine 0.012 < floor 0.150 (state is off the anchor manifold; guessing a number would be worse than none)",
    "id": "649c0fe993ada1f0"
  },
  "escalate": {
    "type": "noul",
    "confident": true,
    "abstained": false,
    "ranked": [
      [
        "no",
        0.5173416321232455
      ],
      [
        "yes",
        0.4826583678767545
      ]
    ],
    "margin_gap": 0.034683264246490986,
    "p": null,
    "calibrated": false,
    "basis": "examples",
    "value": false,
    "support": 0.5173416321232455,
    "id": "76b5d232b5f47146"
  }
}
```


Answer fields: `ranked` (option, score), `value` (None when abstained — `why` says why), `margin_gap`,
`confident`, `p` (None until calibrated), `set` / `set_alpha` (conformal), `via` (`typed` | `reflex` |
`escalated`), `id`. `systemone_map(states, ...)` returns one such dict per state; `systemone_stream(rows,
questions, lr=1.0)` runs prequentially and returns `prequential_accuracy`, `drift`, `calibrated`.

More doors on the same fitted model (kept per schema, so outcomes accumulate): `systemone_batch_fdr(states,
questions, question, alpha, n_null=200)` → `{accepted, p, q, n}` (BH over shuffle-null p-values; FDR 0.03 at
nominal 0.05 measured; use ≥ 200 nulls); `systemone_absorb(states, questions, question)` → guarded EM over
unlabeled states (refused above 10 options; kept only if held-out accuracy did not fall; plain NB + EM
0.697 → 0.752 on AG News k=32).

## 3. Outcomes by id — the only outcome path

`decision_outcome(id, truth)` reports what actually happened. For a typed decision it forwards to the fitted
model's `observe` (the count table learns; prequential 0.699 → 0.764 with zero model calls), writes the
experience to the lever-7 reflex trace, and feeds `calibrate_reflex`. Report `"failed"` (or `None`) when NO
truth is known — that, and only that, marks the reflex's failure field; a correction is written as the truth.

### decision_outcome(id, truth) -> report
```json
{
  "id": "2c8bf88bf5dae22a",
  "outcome": "shipping",
  "forwarded": {
    "was_correct": true,
    "updated": true
  },
  "was_correct": true,
  "reflex": {
    "learned": true,
    "was_correct": true,
    "write": {
      "accepted": true,
      "surprise": 1.0,
      "load": 0.00048828125,
      "tiles": 1
    },
    "calibrated": null
  }
}
```


`decision_records(k)` lists the ledger with `stats`; `decision_ledger()` is the store (`similar(id)` finds a
like decision by cosine of the HRR-encoded records); `decision_ledger_to_memory()` teaches every reported
record into the partition so `ask("decision: <question> :: <state>")` recalls it at T0.

## 4. Routing without a bare rejection: `route_tiered`

`route_tiered(problem, k=5, z_answer=-0.1, z_refuse=-0.5, clarify=False, reflex=False, verify=False)` returns
one of three tiers. Measured on held-out paraphrases ablated from the index: the old gate accepted 4.7%;
tiered answers 32% at 0.76, menus 31.6% (the right card in the menu 83%), refuses 36% — and 0 of 30
off-catalog probes answered. `clarify=True` restores the family question (measured worse than the menu).

### route_tiered -> an answer tier (options cut to 2)
```json
{
  "tier": "answer",
  "answer": "Smooth a bumpy mesh surface (Taubin no-shrink)",
  "z": 4.019274562376419,
  "score": 9.5,
  "ties": 1,
  "families": [
    "mesh_and_geometry"
  ],
  "options": [
    {
      "name": "Smooth a bumpy mesh surface (Taubin no-shrink)",
      "score": 9.5,
      "family": "mesh_and_geometry",
      "method": "mesh_smooth"
    },
    {
      "name": "Query expansion gated on faithfulness",
      "score": 3.0,
      "family": null,
      "method": "attach_llm"
    }
  ],
  "p": 0.015384615384615385,
  "id": "10c1f20783fc7993",
  "question": null,
  "reason": "z=4.02 clears -0.10 and the top score stands alone"
}
```

### route_tiered -> a menu tier (options cut to 3)
```json
{
  "tier": "refuse",
  "answer": null,
  "z": -0.6335290653874975,
  "score": 2.5,
  "ties": 5,
  "families": [
    "mesh_and_geometry"
  ],
  "options": [
    {
      "name": "Depth map to a clean height-field mesh",
      "score": 2.5,
      "family": null,
      "method": "fuse_depth"
    },
    {
      "name": "Mesh as a sequence (SATO-SEQ: stable serialization + hypervector encode)",
      "score": 2.5,
      "family": "mesh_and_geometry",
      "method": "mesh_to_tokens"
    },
    {
      "name": "mesh_to_field",
      "score": 2.5,
      "family": "mesh_and_geometry",
      "method": "mesh_to_field"
    }
  ],
  "p": 0.7538461538461538,
  "id": "c03193db9458eeaf",
  "question": null,
  "reason": "z=-0.63 is in the gibberish band (< -0.50)"
}
```


`catalog_families()` → `{name: (family, "module"|"decided"|None)}` (501 of 887 resolve; the rest reported,
never guessed). `decision_tree(context, depth, fanout, reflex=False)` grows a contingency tree; each node's
`options[path]` carries `context`, `ranked`, `id`. `plan_from_request(text)` splits a compound request into
clauses, routes each, and chains them into one plan vector:

### plan_from_request -> steps
```json
{
  "steps": [
    {
      "clause": "smooth a bumpy mesh",
      "tier": "answer",
      "tool": "Smooth a bumpy mesh surface (Taubin no-shrink)",
      "id": "ed9e7617559d7cf0",
      "options": []
    },
    {
      "clause": "grow crystals on a surface",
      "tier": "answer",
      "tool": "Grow crystals (on a surface, as a cluster, in a geode, gated by a field)",
      "id": "43cd36cc850e114f",
      "options": []
    }
  ],
  "root": "PlanNode(action='Smooth a bumpy mesh surface (Taubin no-shrink)', scope='global', confidence=1.0, branches={abstain, ok}"
}
```


## 5. The reflex arc learning from use

Opt in with `reflex=True` on `route_tiered`, `decision_tree`, `systemone_decide`. The reflex answers only
behind the SEEN gate — a reported decision key within cosine 0.8 of the query (keys: the routing fingerprint
for routes and trees, the state's n-grams for typed decisions) — because trace confidence alone cannot tell a
repeat from a wrong fire. Measured on the router with both paraphrases ablated: repeats answered from
experience 99.3% at 0.982; new paraphrases 20.9% at 0.81. For typed decisions the count table stays the
learner (the reflex measured worse there), so it is off by default.

### route_tiered(reflex=True) after the outcome: the repeat answered from experience
```json
{
  "tier": "answer",
  "answer": "Smooth a bumpy mesh surface (Taubin no-shrink)",
  "via": "reflex",
  "p": null,
  "confidence": 0.5246689212697481,
  "id": "86d4fd25dc4d017c",
  "reason": "answered from experience (reflex confidence 0.52)"
}
```


`reflex_learn(id)` is what the outcome path calls; `reflex_decide(state, key)` is the raw read; `reflex_retile
(advisory_load=0.03)` rebuilds the trace at the measured capacity cliff (a trace loaded by `boot` under the old
tiling reads back under the floor; the first reflex read after boot re-tiles itself).

## 6. Verifying a served answer: `verify_decision`

`verify_decision(state, answer, key="ngram"|"fingerprint")` → `{valid, score, checks, why}`. Five checks with
evidence: `forward` (the trace read with the state cleans up to the answer), `backward` (the trace read with the
answer atom points back at the state — the bidirectional lookup), `profile` (displacement against reported-
correct pairs), `seen`, `drift_z` (support against the recent stream; < −2σ vetoes). Both lookups are RELATIVE
to the other labels (`forward_margin`, `backward_margin`) — an absolute floor let a wrong label through on a
loaded trace. Measured: a seen state served the truth verifies 0.97–0.99 valid, a wrong label 0.00 (forward and
backward AUROC 1.000). It is a verdict, not a confidence: the calibrated margin ranks. `valid` is `None` when
there is no experience to check against — never a guess. `route_tiered(..., verify=True)` attaches it.

### verify_decision(state, answer) -> verdict
```json
{
  "valid": true,
  "score": 0.6899607745814669,
  "checks": {
    "forward": 0.5445074786688247,
    "backward": 0.5208238405645487,
    "forward_margin": 0.5443621848265062,
    "backward_margin": 0.5206885156262261,
    "profile": 0.694511779094494,
    "seen": 0.999999999998,
    "drift_z": null
  },
  "why": "forward 0.54 and backward 0.52 agree and beat every other label by 0.54 / 0.52"
}
```


## 7. The swarm step contract (NOOA's validated termination)

`swarm_step(state, tool, args, done_when, evidence, worker, topic="swarm", verify=None, expect=None)` is
REFUSED without `done_when` and `evidence`. With `verify={"verb": <mind verb>, "args": {...}}` and `expect`
(a value, or a callable over the result), the harness RUNS the verification before the step is accepted; a
failed verification never reaches the bus. Accepted steps are DecisionRecords (via `swarm`) published on the
mind's MessageBus. `swarm_evaluate(audits=(...))` runs `reachability_audit`, `catalog_gaps`, `skill_lint` as
subprocesses and returns `all_ok` — the evaluator role and the hard exit.

### swarm_step -> the published record (verify ran BEFORE acceptance)
```json
{
  "id": "0c2faafd5aecfe00",
  "state": "resolve families",
  "question": "step:catalog_families",
  "options": [
    "catalog_families"
  ],
  "answer": "catalog_families",
  "via": "swarm",
  "margin": null,
  "p": null,
  "cost": null,
  "outcome": null,
  "meta": {
    "args": {
      "decide": false
    },
    "done_when": "at least 500 resolve",
    "evidence": {
      "resolved": 501
    },
    "worker": "w1",
    "verify": {
      "verb": "catalog_families",
      "args": {}
    },
    "verified": {
      "verb": "catalog_families",
      "passed": true
    }
  },
  "evidence": {
    "resolved": 501
  },
  "verify": {
    "verb": "catalog_families",
    "args": {}
  }
}
```


### decision_records(k=3) -> ledger view
```json
{
  "records": [
    {
      "id": "0c2faafd5aecfe00",
      "state": "resolve families",
      "question": "step:catalog_families",
      "options": [
        "catalog_families"
      ],
      "answer": "catalog_families",
      "via": "swarm",
      "margin": null,
      "p": null,
      "cost": null,
      "outcome": null,
      "meta": {
        "args": {
          "decide": false
        },
        "done_when": "at least 500 resolve",
        "evidence": {
          "resolved": 501
        },
        "worker": "w1",
        "verify": {
          "verb": "catalog_families",
          "args": {}
        },
        "verified": {
          "verb": "catalog_families",
          "passed": true
        }
      },
      "evidence": {
        "resolved": 501
      },
      "verify": {
        "verb": "catalog_families",
        "args": {}
      }
    },
    {
      "id": "ed9e7617559d7cf0",
      "state": "smooth a bumpy mesh",
      "question": "route",
      "options": [
        "Smooth a bumpy mesh surface (Taubin no-shrink)",
        "Query expansion gated on faithfulness",
        "mesh_smooth"
      ],
      "answer": "Smooth a bumpy mesh surface (Taubin no-shrink)",
      "via": "route",
      "margin": 4.019274562376419,
      "p": 0.015384615384615385,
      "cost": null,
      "outcome": null,
      "meta": {
        "tier": "answer"
      },
      "evidence": null,
     
  ... (truncated)
```


## 8. The NOOA properties, and where each lives

| NOOA (arXiv 2607.20709) | on these doors |
|---|---|
| validated termination — typed result with evidence and a verification command checked before return | `swarm_step(verify=, expect=)`; `decide_or_escalate` validates the model end's reply to the schema, one retry |
| docstrings as prompts, annotations as contracts | `SystemOne.escalation_prompt` renders the four-part prompt (goal / return format / constraints / verification) from the schema; the payload carries `prompt` and `spec` |
| pass-by-reference with bounded previews | `DecisionRecord.to_dict` ships a state over 400 chars as `{ref, len, preview}` |
| long-term memory (ACT-R activation, decay, reflection) | `decision_ledger_to_memory()` + `memory_curate` |
| model-queryable event history | `decision_records()`, the bus history, `Ledger.similar()` |
| code as action; sandboxed execution | absent by decision (no exec REPL; the engine never runs model-written code) |

The model end: `systemone_decide(..., escalate=callable)` routes an abstention through `decide_or_escalate`;
the callable receives `{question, spec, state, evidence, why, prompt}` and must return one of the options (or
null). Its answer is a record `via="model_end"`; once its outcome is reported, the reflex answers the next
similar task itself — leOS's self-extending instruction (measured: one model call, then none).

## 8b. The code workflow: plan, edit under validated termination, review with evidence

Three doors compose the engine's existing code faculties (`code_search`, `code_similar` -- Rule 0 asked of
the source -- `function_purity`, `affected_tests`, `file_python_check` / `file_import_check` /
`file_selftest`, `file_undo`, the audits) into the path an LLM should follow when it writes code through
leCore. Each is a record; each has a measurement.

**Plan.** `plan_change(request)` gathers the evidence -- the catalog's tier and z, the nearest source by
`code_search`, the family -- and decides reuse / extend / build: the measured tier decides where it is
decisive, modification intent ("add a parameter to X") turns an answer-tier hit into *extend*, and the typed
decision breaks the menu tie and learns from `decision_outcome(id, "reuse"|"extend"|"build")`. The steps are
the build loop with a `done_when` each. Measured on 12 historical requests: 11 of 12 against today's truth
(majority 0.33); the three original misses were two catalog gaps the plan exposed (fixed as aliases) and the
intent rule.

### plan_change -> action, evidence, steps (cut to 3)
```json
{
  "action": "extend",
  "via": "typed",
  "evidence": {
    "catalog_tier": "menu",
    "catalog_top": "systemone_absorb",
    "z": 0.33639853193254887,
    "source_top": "modeltrain.BehaviorPool.add",
    "source_similarity": 0.16666666666666666,
    "family": "agents_and_reasoning"
  },
  "family": "agents_and_reasoning",
  "steps": [
    {
      "step": "extend",
      "do": "add a default-off parameter or method to modeltrain.BehaviorPool.add",
      "done_when": "existing decisions bit-identical; the new path pinned in the selftest"
    },
    {
      "step": "wire",
      "do": "a thin delegating UnifiedMind method, default-off",
      "done_when": "the verb is on the mind and reachable over /invoke"
    },
    {
      "step": "register",
      "do": "register_capability with a runnable example and user-phrased aliases",
      "done_when": "find_capability on 5 stranger phrasings resolves it at top-1"
    }
  ],
  "id": "8cd20932573f816b",
  "ranked": [
    [
      "build",
      0.2208944082426068
    ],
    [
      "extend",
      0.22005520580256316
    ],
    [
      "reuse",
      0.12963495916796386
    ]
  ]
}
```


**Edit.** `edit_verified(path, old, new, count=1, import_check=False, selftest_module=None)` is one
`file_replace` under NOOA's validated termination: syntax always, the import in a subprocess and the module
selftest when asked; ANY failure undoes the edit and refuses with the failing check attached. Measured on 200
synthetic edits: 100 of 100 broken edits refused and restored byte-identical, 100 of 100 good edits accepted.

### edit_verified -> a REFUSED edit (the file was restored)
```json
{
  "ok": false,
  "path": "m.py",
  "checks": {
    "replace": {
      "path": "m.py",
      "replacements": 1,
      "first_line": 3
    },
    "python_check": {
      "ok": false,
      "error": "line 3: '(' was never closed"
    }
  },
  "undone": {
    "undone": 1,
    "files": [
      "m.py"
    ]
  },
  "restored_byte_identical": true,
  "why": "python_check: line 3: '(' was never closed"
}
```


**Review.** `review(paths, duplicates=True, purity=True, tests=True)` reports, per file: syntax; import;
determinism hazards from the AST with the line and the reason (`hash()`, unseeded random, wall clock, unsorted
`listdir`/`glob`, set iteration -- comments and docstrings cannot trip it); undocumented public defs; functions
over 120 lines; modules over the 2,000-line part cap; a missing selftest; possible duplicates by
`code_similar`; impure functions by `function_purity`; and the tests the change needs. `merge_ready` is a
stated rule -- no errors -- never a score (averaging findings ranked worse). Report `"merged"` or
`"reverted"` against the review's `id`.

### review -> findings with lines and reasons; merge_ready is a rule
```json
{
  "merge_ready": false,
  "files": {
    "bad.py": {
      "findings": [
        {
          "level": "error",
          "kind": "determinism:hash",
          "line": 3,
          "what": "hash() is salted per process -- use hashlib for content hashes",
          "code": "return hash(x)"
        },
        {
          "level": "warn",
          "kind": "determinism:unsorted_fs",
          "line": 6,
          "what": "os.listdir/glob without sorted() -- filesystem order is not deterministic across machines",
          "code": "return time.time(), os.listdir('.')"
        },
        {
          "level": "warn",
          "kind": "determinism:wall_clock",
          "line": 6,
          "what": "time.time()/datetime.now() in a value path -- outputs must not depend on the clock",
          "code": "return time.time(), os.listdir('.')"
        },
        {
          "level": "error",
          "kind": "undocumented",
          "line": 2,
          "what": "public def f has no docstring (undiscoverable)"
        },
        {
          "level": "warn",
          "kind": "no_selftest",
          "line": 0,
          "what": "no _selftest() + __main__ block"
        }
      ],
      "counts": {
        "error": 2,
        "warn": 3
      },
      "merge_ready": false,
      "import": {
        "ok": true,
        "error": null
      }
    }
  },
  "tests": null,
  "id": "5f78a854c86d54e1"
}
```


The loop an LLM should run: `plan_change` -> `edit_verified` per edit -> `review` -> `swarm_evaluate` for the
audits -> `decision_outcome` on the plan and the review with what actually happened. Every step is a record
the next session can find (`decision_records`), and the reflex answers a repeated brief from experience.

## 9. What the numbers are, and are not

Substrate alone on Banking77 is 10–20 points under fine-tuned encoders (0.656 vs 85–87 at 10-shot); with a
model end on the escalated share the combined system reaches 0.90–0.92 at 20-shot with half the model calls.
On calibration, coverage, batch error, off-scope refusal, drift, learning from outcomes and from use, and a
verdict on a served answer, these doors report what Jev's public materials do not. An external agentic
benchmark (NOOA's) is still open: SWE-bench Verified file localisation runs here (0.436 top-1, no model; the
model end 9 of 12 on the escalated share); `resolved` needs Docker and a coding model end. See
`docs/research/BENCHMARK_sweep176.md`.
