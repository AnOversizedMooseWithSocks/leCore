# Faculty map -- UnifiedMind's 1329 public methods, by topic

*Generated by `facultymap.py` from live introspection -- do not edit by hand; regenerate instead.*
*123 topical clusters (prefix, >= 3 methods) + an alphabetical tail of 588.*

## Topics

- [adaptive](#adaptive) (5)
- [advance](#advance) (3)
- [affine](#affine) (4)
- [apply](#apply) (3)
- [ascii](#ascii) (4)
- [attribute](#attribute) (3)
- [auto](#auto) (5)
- [bake](#bake) (11)
- [blend](#blend) (6)
- [brep](#brep) (5)
- [build](#build) (8)
- [candle](#candle) (4)
- [chunk](#chunk) (9)
- [classify](#classify) (6)
- [cloud](#cloud) (3)
- [code](#code) (6)
- [compare](#compare) (3)
- [compile](#compile) (5)
- [complete](#complete) (4)
- [compose](#compose) (5)
- [compress](#compress) (3)
- [curve](#curve) (7)
- [decide](#decide) (3)
- [decode](#decode) (6)
- [decompose](#decompose) (7)
- [delta](#delta) (5)
- [denoise](#denoise) (3)
- [depth](#depth) (3)
- [detect](#detect) (3)
- [diffuse](#diffuse) (5)
- [directed](#directed) (3)
- [discover](#discover) (3)
- [distribute](#distribute) (3)
- [domain](#domain) (4)
- [encode](#encode) (6)
- [encyclopedia](#encyclopedia) (7)
- [exact](#exact) (3)
- [explain](#explain) (5)
- [falsecolor](#falsecolor) (3)
- [field](#field) (8)
- [file](#file) (21)
- [fillet](#fillet) (3)
- [find](#find) (6)
- [fit](#fit) (6)
- [fluid](#fluid) (3)
- [forecast](#forecast) (3)
- [fractal](#fractal) (7)
- [frame](#frame) (4)
- [fuse](#fuse) (4)
- [game](#game) (3)
- [gather](#gather) (5)
- [generate](#generate) (8)
- [gradient](#gradient) (3)
- [graph](#graph) (5)
- [guide](#guide) (4)
- [harmonic](#harmonic) (4)
- [holographic](#holographic) (3)
- [hypervector](#hypervector) (3)
- [identify](#identify) (3)
- [image](#image) (10)
- [is](#is) (6)
- [job](#job) (6)
- [ladder](#ladder) (4)
- [learn](#learn) (15)
- [load](#load) (6)
- [machine](#machine) (6)
- [make](#make) (10)
- [manifold](#manifold) (4)
- [mantis](#mantis) (3)
- [material](#material) (11)
- [materials](#materials) (3)
- [measure](#measure) (6)
- [mesh](#mesh) (72)
- [navigate](#navigate) (3)
- [nbody](#nbody) (4)
- [nebula](#nebula) (3)
- [nurbs](#nurbs) (4)
- [phase](#phase) (4)
- [plan](#plan) (8)
- [postfx](#postfx) (4)
- [quantum](#quantum) (7)
- [query](#query) (3)
- [ray](#ray) (4)
- [read](#read) (4)
- [realize](#realize) (3)
- [recall](#recall) (4)
- [recipe](#recipe) (3)
- [recognize](#recognize) (3)
- [region](#region) (3)
- [register](#register) (5)
- [render](#render) (22)
- [replay](#replay) (3)
- [resolve](#resolve) (5)
- [rm](#rm) (4)
- [route](#route) (5)
- [run](#run) (11)
- [sample](#sample) (8)
- [save](#save) (3)
- [scan](#scan) (3)
- [scatter](#scatter) (6)
- [scene](#scene) (15)
- [sdf](#sdf) (16)
- [select](#select) (6)
- [set](#set) (5)
- [skin](#skin) (3)
- [sky](#sky) (6)
- [smoke](#smoke) (4)
- [smooth](#smooth) (4)
- [snap](#snap) (5)
- [soft](#soft) (8)
- [solve](#solve) (9)
- [spectral](#spectral) (12)
- [splat](#splat) (13)
- [stokes](#stokes) (7)
- [stream](#stream) (5)
- [suggest](#suggest) (4)
- [surface](#surface) (6)
- [synthesize](#synthesize) (3)
- [texture](#texture) (4)
- [validate](#validate) (5)
- [verify](#verify) (3)
- [workflow](#workflow) (3)
- [zig](#zig) (4)

## adaptive

- **`adaptive_anchors`** -- Place n cache/codebook anchor positions along `x` so they crowd where the field `y` bends (high
- **`adaptive_conformal`** -- Forecasting backlog (F2): temporal conformal for time series (which break exchangeability). Adaptive
- **`adaptive_pipeline`** -- MEASUREMENT-DRIVEN adaptive dispatcher: run identify_level, then route the data to the method its
- **`adaptive_record`** -- A role->filler memory whose representation is GATED BY LOAD and FIDELITY NEED (the FHRR/tensor re-enables):
- **`adaptive_sample_budget`** -- Forecasting sweep (sec.5, renderer delegation): a CALIBRATED adaptive-sampling stop. Given a renderer's

## advance

- **`advance_bank`** -- Advance M island variants k substeps in ONE batched closed form: (M,2n) -> (M,2n). Measured, M=32 x 1,920
- **`advance_ccd`** -- Advance points one step WITHOUT tunnelling: sweep to first contact, stop there, cancel the into-surface
- **`advance_particles`** -- One integration step for an N-D particle set (gravity / attractor / sampled-field force). See holographic_emitter.

## affine

- **`affine_compose`** -- Collapse a chain of (s, t) edits into ONE canonical (S, T) by the affine group law -- exact, not an
- **`affine_jump`** -- THE MODAL JUMP, stateless: advance the affine recurrence `s <- A s + b` by k substeps in ONE
- **`affine_limit`** -- The k -> infinity fixed point of `s <- A s + b` (a settled island), computed per-mode so it can refuse
- **`affine_normality`** -- **Where the transform tower's mechanism stops.** {in_affine, in_projective, conjugate_is_affine}.

## apply

- **`apply`** -- Write a merged delta ({slot: vector}, e.g. from mind.merge_forks(...)["merged"]) back into a shared world.
- **`apply_in_superposition`** -- Fill 3: the latency-hiding move -- hold items in superposition and apply ONE bind by `op` to each
- **`apply_mueller`** -- Transform a Stokes vector/field by a Mueller matrix (or a per-pixel matrix-field). See

## ascii

- **`ascii_animate`** -- Render an ASCII ANIMATION to a list of `n` text frames (holographic_ascii.ascii_frames) -- the
- **`ascii_field`** -- Project a 2-D scalar FIELD sampler straight to TEXT (holographic_ascii.ascii_field) -- composability
- **`ascii_sdf`** -- Preview a 3-D SDF scene as TEXT -- raymarch + shade + ASCII in one call (holographic_ascii.ascii_sdf),
- **`ascii_view`** -- Render any image to TEXT at `width` characters -- the terminal/log/SSH projection backend

## attribute

- **`attribute`** -- WHO taught this? If the sequence model was fit on (text, source)
- **`attribute_field`** -- G6 -- a per-vertex/texel attribute as a RESOLUTION-INDEPENDENT field over the surface domain.
- **`attribute_sources`** -- Source attribution: trace which stored material a passage drew on. sources

## auto

- **`auto_bump`** -- Inverse-rendering IR1: derive a plausible tangent-space normal map (and height) from an albedo image
- **`auto_displace`** -- Inverse-rendering IR5: promote an auto-bump height (IR1) from a shading bump to REAL geometry -- move a
- **`auto_fuse_depth`** -- AUTO-WEIGHTED depth fusion: combine the HAZE and SHARPNESS cues, each weighted by how well it AGREES
- **`auto_retopo`** -- AUTO-RETOPO: turn a messy BLOCK-OUT (a skin_skeleton blob, metaball, boolean mess) into a clean quad-
- **`auto_scale`** -- Automatic scaling (holographic_scalinglaw): repeatedly diagnose from the

## bake

- **`bake`** -- CONSOLIDATION CACHE (H2) -- bake a slow `evaluator` over the thing that VARIES, then look it up cheaply.
- **`bake_cloud_job`** -- Start the SLOW part of make_cloud (the fBm noise bake -- grid=32 ~60s) as a real background JOB you can
- **`bake_deformation`** -- Evaluate an animation into a FrameCache: for each frame f, cache frame_fn(base, f) as a delta. Returns
- **`bake_field`** -- H3 -- bake a sampled function into ONE hypervector ("the texture unit"): F = sum f(x_i) Z(x_i). Fetch any
- **`bake_field_nd`** -- H5 -- the texture unit in N dimensions: a gridded function baked into ONE hypervector, with the per-axis
- **`bake_material`** -- Bake a material's VIEW-INDEPENDENT channels into field lookups (MC2): a procedural texture becomes a
- **`bake_normal_map`** -- BAKE a normal map (optionally AO) from a HIGH-poly onto a LOW-poly UVs -- keep the sculpt detail on the
- **`bake_scene`** -- PRECOMPUTE / BAKE a scene BEFORE any render, so the first render is already a relight, not a cold trace. Call
- **`bake_sdf`** -- PRECOMPUTE a scene SDF (anything with `.eval`, and optionally `.ids`) onto a grid, then sample it O(1) --
- **`bake_texture`** -- Bake a CMP1 texture graph to a res x res grid -> a BakedTexture you sample in O(1) (bilinear lookup),
- **`bake_view_lut`** -- Pre-integrate the view-DEPENDENT specular (MC3): bake directional_albedo over a (view_cos, roughness)

## blend

- **`blend`** -- PROJECTION TO CREATE NEW THINGS, over the mind's OWN learned classes.
- **`blend_forcings`** -- The one thing that DOES superpose exactly: variants differing only in their FORCING. `s_k = A^k s0 +
- **`blend_images`** -- 2D GENERATE -- a crossfade/morph sequence between two images (the midpoint is the 0.5*a+0.5*b double
- **`blend_pose`** -- The forward blendshape/skinning map for STRUCTURES (holographic_blendpose, ARCH-6): a soft weighted blend
- **`blend_programs`** -- BLEND two program signatures into one by bundling -- composition in the shared substrate. The blend
- **`blend_shapes`** -- Morph-target / blendshape mix as a WEIGHTED BUNDLE: base + sum_i w_i (target_i - base). `base` and each

## brep

- **`brep_boolean`** -- The FINISHED B-REP BOOLEAN (union/difference/intersection): the SSI-driven re-stitch that turns two
- **`brep_boolean_faces`** -- B-REP BOOLEAN CLASSIFICATION (toward K6 booleans): which whole FACES of A survive a boolean with B
- **`brep_box`** -- B-REP (K6): construct a valid closed cube boundary representation (8 verts, 12 edges, 6 faces, genus 0).
- **`brep_from_faces`** -- B-REP (K6): build a boundary representation from vertices + BFace loops (topology derived and checkable).
- **`brep_validate`** -- B-REP (K6): validity report for a boundary representation -- {closed_manifold, V,E,F,R,S, genus,

## build

- **`build_index`** -- CONSOLIDATION INDEX (H1) -- build a nearest-neighbour index over `vectors` with one interface: an exact
- **`build_meaning_predictor`** -- Give the mind a MEANING-level predictor: instead of returning a single
- **`build_occlusion_forest`** -- Build a HoloForest over `codebook` for forest-routed occlusion selection (holographic_occlusion, SPEED-2,
- **`build_occlusion_gram`** -- The cached Gram matrix for occlusion_recall's fast path (holographic_occlusion, SPEED-1) -- G = codebook @
- **`build_predictor`** -- Give the mind a PREDICTIVE LOOP over symbol sequences: it anticipates
- **`build_prototypes`** -- Build class prototypes from EXAMPLES: {class: [example, ...]} -> {class: unit mean vector} (the mean
- **`build_scene`** -- DESCRIBE a scene in plain words and get back a LIVE, adjustable scene the system built for you -- a
- **`build_semantic_index`** -- Build an OPT-IN semantic index over the dictionary so you can find words by MEANING (idx.find('unexpected

## candle

- **`candle_carrier`** -- Turn OHLC candles into the one-value-per-bar WAVE they sample (holographic_candles): a price candle is a
- **`candle_envelope`** -- The high/low BAND around a candle series' carrier (holographic_candles) -- how far the price wave swung
- **`candle_intrabar_path`** -- Reconstruct a HIGHER-RESOLUTION wave from OHLC candles (holographic_candles): each bar becomes O ->
- **`candle_range`** -- Per-bar RANGE (High-Low, the within-bar swing amplitude) and BODY (|Close-Open|) of a candle series

## chunk

- **`chunk_byte_report`** -- The codec comparison, carried WITH the capability because the token ratio invites a claim the bytes do
- **`chunk_codebook`** -- Rehydrate a plain-data codebook into a live ChunkCodebook (with .encode/.decode/.stats). The in-process
- **`chunk_codebook_vectors`** -- THE R3 BRIDGE: realize a LEARNED chunk codebook (mind.learn_chunks, R1) as the chunk vectors the
- **`chunk_coverage`** -- What fraction of the groups actually used are PRESENT in the learned chunk codebook?
- **`chunk_decode`** -- Expand chunk tokens back to base symbols. LOSSLESS: chunk_decode(chunk_encode(s)) == s exactly.
- **`chunk_encode`** -- Tokenize `stream` against a learned codebook by replaying its merges in learning order. Returns a token
- **`chunk_levels`** -- The chunk depths a learned codebook can factor against, DEEPEST FIRST -- the ladder recursive_factor
- **`chunk_route`** -- Store/replay an EXPLICIT ordered sequence you ALREADY HAVE -- a GPS route from a planner, a fixed
- **`chunk_stats`** -- {n_symbols, n_tokens, token_ratio, mean_depth, max_depth, n_merges, covered}. `token_ratio` is a TOKEN

## classify

- **`classify`** -- Nearest self-organized prototype. If `route` is on, the query competes only
- **`classify_cloud`** -- Classify every point of a cloud into void/filament/wall/node and return (labels, info, summary) -- the
- **`classify_contact`** -- Name a contact's TYPE (bounce / slide / rest_contact / penetration / jam) from its {overlap,
- **`classify_equivariance`** -- The strongest verdict that holds, measured. A `recompute` result means the REGISTERED law failed -- which
- **`classify_robust`** -- MULTI-RAY classification: one query is one noisy ray; fire several
- **`classify_transform`** -- **THE ONE ENTRY POINT: which floor of the transform tower is this on?** Give it any callable on points;

## cloud

- **`cloud_report`** -- The bar, carried with the capability: {evals_closed, evals_reference, eval_ratio, max_error, mean_error}.
- **`cloud_single_scatter`** -- Single-scattered radiance, returning (radiance, density_evals). The VIEW ray must march -- its integrand
- **`cloud_transmittance`** -- Beer-Lambert transmittance exp(-sigma_t * tau) with `tau` from ONE closed-form integral -- O(1) in march

## code

- **`code_byte_report`** -- The codec comparison, carried WITH the capability: {raw, zlib_raw, codebook_bytes, delta_bytes,
- **`code_decompose`** -- Split a statement into (shape_template, delta): the AST with every identity slot blanked, and the ordered
- **`code_rebuild`** -- Rebuild normalized source from (codebook, stream). "Normalized" is precise, not a hedge: `ast.unparse` is
- **`code_recompose`** -- The exact inverse of code_decompose. Raises on a delta of the wrong length -- a silent short-read would
- **`code_shape_census`** -- {statements, distinct_kept, distinct_erased, reuse_kept, reuse_erased, singleton_fraction} over a source's
- **`code_structure`** -- A module's top-level statements as (codebook, stream): shape_key -> template, and [(key, delta), ...].

## compare

- **`compare_image_files`** -- Perceptual similarity in [0,1] (1 = identical) between two images given as FILE PATHS (e.g. two rendered
- **`compare_images`** -- Inverse-rendering IR4: a PERCEPTUAL render-vs-target similarity in [0,1] (1 = identical) -- multi-scale
- **`compare_structures`** -- Superpose two assembled structures (assemble() outputs) and read their OVERLAP -- the Baker seat's

## compile

- **`compile_cache_stats`** -- Stats for the process-wide compile cache: hits/misses/compiles/evictions, size, hit_rate. See
- **`compile_material`** -- Compile a material's socket graph into ONE cached shade(points)->channels kernel (MC1): built once, keyed
- **`compile_pipeline`** -- Compile a render/sim pipeline's plan ONCE per config (PW2): select+auto-include+toposort the stages,
- **`compile_program`** -- Assemble a HoloMachine program (list of (opcode, operand)) into its program vector ONCE via the compile
- **`compile_structure`** -- Compile a structure-description spec (ISA-7) to a StructureRecipe at this mind's dim/seed. The spec is

## complete

- **`complete_instruction`** -- Predict the next full INSTRUCTION -- opcode AND operand -- from the learned recipe grammar (the
- **`complete_method`** -- Method-name AUTOCOMPLETE: this mind's methods starting with `prefix`, each with its signature -- what an
- **`complete_object`** -- Inverse-rendering IR11: given a partial FRONT view of an object and an ObjectArchive of complete objects,
- **`complete_procedure`** -- Predict the next opcode given a partial recipe (a list of (opcode, operand) or bare opcodes),

## compose

- **`compose_from_tags`** -- FORWARD compositional generation: bind each object's (colour, shape, texture) tags into a composite
- **`compose_mueller`** -- Fold a light path (elements IN THE ORDER LIGHT PASSES THROUGH) into one Mueller matrix. See
- **`compose_nested`** -- Fractal composition -- the SAME bind+superpose that builds a scene from objects,
- **`compose_scene`** -- Run the resonator FORWARD on this mind's scene coder: bind chosen attribute
- **`compose_word`** -- Compose a chain of 4x4 transforms into ONE. **A group is CLOSED, so the "word" IS a "letter"** -- drawn

## compress

- **`compress_cost`** -- Better structure means better compression, measured: encode a sequence by
- **`compress_lossless`** -- Go both directions: losslessly compress a sequence to a compact code (seed
- **`compress_tensor`** -- A7/M1 -- compress MULTI-WAY data (a field over x,y,t; a frame stack; a BRDF table; a volume) by factoring

## curve

- **`curve_bezier`** -- Evaluate a BEZIER curve of any degree at parameter(s) u in [0,1] by de Casteljau (numerically stable).
- **`curve_bspline`** -- Sample a uniform B-SPLINE (default cubic) over `control` -- the smoothest (C^(degree-1)) of the
- **`curve_catmull_rom`** -- Sample a CATMULL-ROM spline that INTERPOLATES `control` (passes through every point) -- the right
- **`curve_frame`** -- Orthonormal FRAME (tangent, normal, binormal) at each point of a 3-D curve. `minimizing=True` gives
- **`curve_intersect`** -- CURVE-CURVE intersection (K1): all crossings of two polylines A,B as records {point,i,j,t,u} (segment
- **`curve_resample_arc_length`** -- Resample a curve to `n` points spaced by EQUAL ARC LENGTH (evenly along the curve, not the parameter)
- **`curve_self_intersect`** -- Self-intersections of one polyline (what an offset curve must clean up). See

## decide

- **`decide`** -- Decide an action. `senses`/`avoid` pass straight through to the
- **`decide_confidence`** -- Decide an action AND report a CALIBRATED confidence in it: (action, pvalue). The p-value is the
- **`decide_or_abstain`** -- The shared DECISION step: given ranked [(name, score)] from match_record / match_prototype / any

## decode

- **`decode_axial`** -- Recover the axial value in [0, pi) from an axial hypervector -- the inverse of
- **`decode_path_step`** -- Read waypoint i back out of a path hypervector -- the route survives as composable VSA data.
- **`decode_plan`** -- Decode a plan vector back to a PlanNode GIVEN ITS SHAPE (from plan_shape) -- schema-guided, so it
- **`decode_record`** -- Decode a flat record GIVEN ITS SHAPE: schema = {field_name: [possible_value_names]} (the per-field
- **`decode_step`** -- Read the i-th instruction of a procedure as (opcode, operand) -- the program-as-DATA query
- **`decode_structure`** -- DECODE a composed CHAIN structure by iterated unbinding with PER-PEEL CLEANUP (B8) -- the

## decompose

- **`decompose_nested`** -- Invert compose_nested: for each group key, unbind its sub-scene out of the
- **`decompose_piecewise`** -- Decompose a PIECEWISE signal (holographic_scaffold): segment at the
- **`decompose_scene`** -- Factor a scene vector back into its per-object attribute tags -- the backward
- **`decompose_scene_tiled`** -- Factor a scene too big to recover whole, by TILING (chunking-transfer item X1). A many-object scene
- **`decompose_signal`** -- Take a foreign 1-D signal APART into the law that generates it -- the measured-regime twin
- **`decompose_structure`** -- Recover the generating recipe of a COMPOSED structure -- the canonical, higher-capacity
- **`decompose_transform`** -- Modeling-app backlog (item G): split a 4x4 transform into (translate, rotation quaternion, scale) --

## delta

- **`delta_cache`** -- A cache whose key is (operator, canonical class, the delta-IDs the operator READS). A material delta
- **`delta_cache_report`** -- Every keying policy against the brute baseline: {policy: {computes, speedup, max_abs_diff,
- **`delta_chain`** -- A chunked DELTA CHAIN (DELTA-1): store a SEQUENCE of (N, D) chunks as a base + per-chunk deltas, each
- **`delta_reshade_move`** -- Apply a MOVE of object `obj_id` by `delta` as a bounded, bit-exact delta: re-shade only the rays that
- **`delta_reshade_scene`** -- Apply a material/colour/light EDIT as a bounded delta: re-shade ONLY the pixels whose ray touched a changed

## denoise

- **`denoise`** -- Clean a noisy signal by projecting it onto a manifold -- Milanfar's thesis that a denoiser
- **`denoise_flow`** -- Denoise an edge flow by keeping only its structurally-valid Hodge components and dropping the rest
- **`denoise_tensor`** -- Tier 5 -- denoise MULTI-WAY data by projecting onto the low-rank Tucker manifold implied by the noise

## depth

- **`depth_fog`** -- Apply exponential DEPTH FOG (Beer-Lambert) to a rendered image (W16): fade each pixel toward
- **`depth_from_image`** -- Estimate a relative DEPTH MAP from a single image by classical SHAPE FROM SHADING (C1 of photo-to-3D) --
- **`depth_to_mesh`** -- DEPTH MAP -> a CLEAN triangulated HEIGHT-FIELD MESH (the mesh-cleanup path for single-view photo-to-3D):

## detect

- **`detect_caustic`** -- Routing-ambiguity (caustic) score at a query: high when the two strongest attractors pull in opposite
- **`detect_drifting`** -- Find a DRIFTING narrowband signal in a spectrogram -- the SETI detection problem (Tarter,
- **`detect_regimes`** -- WHERE does a recorded series change behaviour? Located change-point detection over a whole batch

## diffuse

- **`diffuse_field`** -- Diffuse a scalar/component grid field by the heat kernel via the FFT -- a Gaussian bind on the
- **`diffuse_heat`** -- Spread heat through a temperature FIELD by Fourier conduction (dT/dt = alpha*laplacian(T)) for `steps`
- **`diffuse_periodic`** -- L5/M2 -- evolve dT/dt = alpha*laplacian(T) on a PERIODIC grid to time `t` EXACTLY, in one evaluation:
- **`diffuse_readout`** -- Read out the matter model's LINEAR diffusion sub-step at any time k in ONE evaluation (PW4): diagonalise
- **`diffuse_steady_state`** -- The closed-form limit of unbounded diffusion (PW4): the flat mean field -- every non-DC mode decays, the

## directed

- **`directed_structure`** -- Encode a directed SEQUENCE or GRAPH with a permutation DIRECTION ROLE (RAY-3): the successor of
- **`directed_successor`** -- Recover the successor(s) of a node in a DirectedStructure (RAY-3): perm_inv(unbind(M, node))
- **`directed_traverse`** -- Walk a directed chain FORWARD from `start_index`, gated by recovery confidence -- the directed

## discover

- **`discover_hierarchy`** -- RECURSIVE self-discovery of order. Test the top-level observations for
- **`discover_sequential`** -- SELF-DISCOVERY of order. For every absorbed class, run the permutation
- **`discover_units`** -- Self-discovery of structure: find the units in a raw symbol stream with

## distribute

- **`distribute_bricks`** -- Run `worker(region, cache)` on each tile/brick and PLACE its result at that region -- disjoint, so
- **`distribute_compute`** -- DISTRIBUTED COMPUTATION the holostuff way -- decompose a job into buckets, hand every bucket the same shared
- **`distribute_exact`** -- `distribute`, but BIT-IDENTICAL under any bucketing. `worker(bucket, cache)` must return the bucket's

## domain

- **`domain_bend`** -- Bend an SDF's domain into an arc (iq's opCheapBend): curl a straight beam by `k` radians per unit
- **`domain_fold`** -- Fold an SDF's domain into mirror symmetry (kaleidoscope from one abs(), iq's opMirror). Folding one
- **`domain_repeat`** -- Tile an SDF (or any .eval field) into an INFINITE lattice by folding the query domain (iq's opRep) --
- **`domain_twist`** -- Twist an SDF's domain into a helix (iq's opTwist): rotate space by `k` radians per unit along `axis`,

## encode

- **`encode_pairs`** -- Encode parallel arrays of keys and values -- bundle of bind(key_i, value_i) -- in ONE batched FFT
- **`encode_path`** -- A navigated path -> ONE hypervector, COMPOSABLE in a VSA program (bind to a label, bundle several routes,
- **`encode_plan`** -- Encode a PlanNode contingency tree (a primary action, a scope, named branches each a PlanNode) as ONE
- **`encode_record`** -- Encode a flat record {field_name: value_name} as one vector -- the GENERAL 'bring your own shape'
- **`encode_scene`** -- Encode parsed objects into ONE composable scene hypervector: superpose bind(OBJ_i, record_i). Returns
- **`encode_texture`** -- The texture graph as ONE hypervector (this mind's dim/seed), for CACHING a baked result by its graph

## encyclopedia

- **`encyclopedia_add`** -- Teach one concept: its is_a PARENT (taxonomy) and/or its HAS parts. Key concepts by a sense id like
- **`encyclopedia_climb`** -- Walk the is_a chain upward as a relation ray: {chain, throughput}. `throughput` is the product of per-hop
- **`encyclopedia_is_a`** -- One hop up the taxonomy: (parent, cleanup_confidence). See holographic_encyclopedia.Encyclopedia.is_a.
- **`encyclopedia_is_a_transitive`** -- Taxonomic membership: does `concept` reach `ancestor` by is_a? {reached, hops, throughput}.
- **`encyclopedia_relatedness`** -- A structural relatedness score in [0, 1]: 1 / (1 + depth_a + depth_b) to the nearest common ancestor.
- **`encyclopedia_reset`** -- Start a fresh encyclopedia (dropping everything taught so far). Returns the number of concepts cleared.
- **`encyclopedia_siblings`** -- Concepts sharing this one's is_a parent -- relatedness from STRUCTURE, not word overlap (which is the

## exact

- **`exact_k_oldest`** -- Select EXACTLY k pixels with the greatest age, ties broken deterministically on the flat index. The
- **`exact_matmul`** -- Exact integer / fixed-point matmul carried over the FHRR phasor algebra -- Path D's arithmetic lever.
- **`exact_sdf_normal`** -- Derive an EXACT SDF surface normal from a symbolic SDF expression (SymPy, design-time) and return

## explain

- **`explain`** -- WHY are two things similar -- not just a cosine, but the per-role
- **`explain_code`** -- Explain source in plain English -- DETERMINISTICALLY (C1): same code, same sentences, every
- **`explain_organization`** -- The whole memory's self-explanation: for every label the organizer
- **`explain_program`** -- Query Interface (Phase 7): EXPLAIN a program WITHOUT running it. A DRY RUN with no handlers -- every
- **`explain_splits`** -- INCEPTION: the mind explains its own memory organization. When the

## falsecolor

- **`falsecolor_handedness`** -- Diverging false-colour for circular polarization sense: right-handed->red, left-handed->blue,
- **`falsecolor_polarization`** -- Standard polarization false-colour: hue=e-vector angle, saturation=degree of linear polarization,
- **`falsecolor_spectral`** -- False-colour N spectral-band readings (...,nchan) into an RGB image, with UV bands made VISIBLE in a

## field

- **`field_deflect`** -- Slide a query toward a local mass concentration in a field of attractors -- a soft, continuous cousin of
- **`field_displace`** -- Displace a mesh's vertices along their normals by a SCALAR FIELD or SDF sampled at each vertex
- **`field_divergence`** -- The divergence field of a velocity grid (compression/expansion), computed spectrally.
- **`field_divergence_3d`** -- The 3-D divergence field of a velocity grid, computed spectrally.
- **`field_effect`** -- Render/Sim Pipeline (Part 4): a shaped zone of influence -- the SDF is the shape, its distance is the
- **`field_report`** -- {lambda_min, energy, n_singularities, sum_index, euler, quarter_residual, poincare_hopf}. JUDGE A FIELD
- **`field_singularities`** -- The STATELESS one-shot twin of `cross_field` + `singularity_index`: mesh in, plain data out
- **`field_to_splats`** -- Pull a metaball FIELD's Gaussians directly as exportable splats (holographic_splatexport, FS-3) -- no fit:

## file

- **`file_archive`** -- Move a file into a timestamped archive dir instead of deleting it (a reversible 'delete'). Returns
- **`file_count`** -- Count occurrences of `text` in a file -- check uniqueness BEFORE file_replace. See Editor.count_occurrences.
- **`file_delete`** -- Permanently remove a file (use file_archive to keep a copy). Returns {deleted}. See
- **`file_delete_lines`** -- Delete lines [start, end] (1-based inclusive). Returns {path, deleted}. See
- **`file_find_definition`** -- Find where a Python function/class `name` is DEFINED under `path` -> [{file, line, kind, text}]. The
- **`file_grep`** -- Search across files under `path` (filtered by `suffix`). Returns [{file, line, text}] -- the 'where is X
- **`file_import_check`** -- Import the module (dotted path under the file root) in a fresh subprocess and report success or the
- **`file_insert`** -- Insert `text` after 1-based line `after_line` (0 = top). Returns {path, inserted_at}. See
- **`file_list`** -- List files under a directory (relative paths); skips __pycache__/hidden. See
- **`file_move`** -- Move/rename a file within the root. Returns {moved_from, moved_to}. See holographic_codeedit.Editor.move.
- **`file_python_check`** -- Syntax-check a .py file (ast.parse only) right after editing it -> {ok, error}. Catch a broken edit
- **`file_read`** -- Read a file's text (utf-8), scoped to the file root. See holographic_codeedit.Editor.read.
- **`file_read_lines`** -- Read lines [start, end] (1-based inclusive) of a file as a list of strings -- look at a region before
- **`file_read_many`** -- Read several files at once -> {path: text} (a bad path maps to an "<error: ...>" string). Gathers
- **`file_replace`** -- Replace EXACT text `old` with `new`; `old` must occur exactly `count` times (count=1 = must be unique,
- **`file_replace_across`** -- Replace exact `old` with `new` in every file under `path` that contains it (dry_run=True previews).
- **`file_replace_lines`** -- Replace lines [start, end] (1-based inclusive) with `text` -- the range edit for when old text isn't
- **`file_tree`** -- An indented directory tree under `path` (skips __pycache__/hidden). Returns a string. See Editor.tree.
- **`file_undo`** -- Reverse the last `steps` mutating file operations (write/replace/insert/delete_lines/replace_lines),
- **`file_view`** -- Read lines [start, end] as one string WITH LINE NUMBERS -- the located form to look at before targeting
- **`file_write`** -- Create or atomically replace a file with `text`. overwrite=False refuses to clobber. Returns

## fillet

- **`fillet_difference`** -- EDGE FILLET (K5): f minus g with the resulting edge rounded to radius r. Returns an SDF callable. See
- **`fillet_intersection`** -- EDGE FILLET (K5): the intersection of f,g with the concave (pocket) crease rounded to radius r
- **`fillet_union`** -- EDGE FILLET (K5): the union of two implicit surfaces f,g with the convex crease rounded to an EXACT

## find

- **`find`** -- Which absorbed record holds bind(role, filler)? One hop over the
- **`find_capability`** -- CONSOLIDATION CATALOG (C1) -- 'search before you build'. Describe a problem in plain English and get the
- **`find_capability_uris`** -- Like find_capability, but each result is annotated with its disambiguating capability URI(s) so a caller
- **`find_materials`** -- Discover materials across BOTH libraries (render presets + physical definitions) by plain-English keywords
- **`find_pattern_by_downscale`** -- Find a pattern in noisy data by DOWNSCALING -- project to a coarse representation where independent
- **`find_scored`** -- Like find_capability, but returns [(capability, score)] -- so a caller can tell a HIT from a FALLBACK.

## fit

- **`fit_base_mesh`** -- FIT A BASE MESH TO A TARGET: skin the skeleton into a base mesh, SHRINKWRAP it onto target_mesh, and
- **`fit_deterministic`** -- Recover the deterministic GENERATOR that best explains a 1-D `data` signal (the inverse of the ladder):
- **`fit_function`** -- Fit an interpretable function y ~ F(X) as a single-layer Kolmogorov-Arnold readout on this
- **`fit_pose`** -- Fit a HUMANOID rig to KEYPOINTS -- the honest 'approximate a pose' (holographic_humanoid). Pass 3-D
- **`fit_primitives`** -- Approximate a (M,3) point cloud with a UNION of PRIMITIVES, best-fit per cluster (holographic_primfit) --
- **`fit_shape`** -- CLOSEST-FIT to a procedural formula + its SHADERTOY / GLSL (holographic_fitshape) -- the capstone. Given a

## fluid

- **`fluid_solver`** -- A grid-based STABLE-FLUIDS solver (Stam 1999) for smoke, buoyant plumes, and combustion/FIRE -- the
- **`fluid_step`** -- One Stable-Fluids step on the torus (add force -> diffuse -> project -> advect), built on the FFT.
- **`fluid_step_3d`** -- One 3-D Stable-Fluids step on a 3-D periodic grid (the same FFT solver, generalised via the n-D real

## forecast

- **`forecast`** -- Forecasting backlog (F3): the "forecast any data" door. Routes a 1-D series to the producer that
- **`forecast_coverage_report`** -- Forecasting backlog (F8): the coverage instrument -- for each alpha, confirm the empirical coverage on
- **`forecast_crps`** -- Forecasting backlog (F8): CRPS -- the proper score for a probabilistic (sample) forecast; coverage says

## fractal

- **`fractal_confidence`** -- A singularity CROSS-CHECK for a 1-D signal's fractal dimension (holographic_bandwidth): two independent
- **`fractal_dimension`** -- The fractal (box-counting) dimension of an input's structure -- a
- **`fractal_field`** -- A multi-scale (fBm-like) tiling: the motif tiled at base_period, /2, /4, ... summed -- richness at
- **`fractal_planet`** -- A data-driven, physically-plausible PLANET as one region field: a fBm-displaced sphere painted by a
- **`fractal_scene`** -- Decode the kernel from a seed hypervector and EXPAND it to a self-similar scene by domain repetition
- **`fractal_seed`** -- Encode a fractal KERNEL into ONE seed hypervector (Quilez: one kernel, a single seed). The kernel is
- **`fractal_volume`** -- ONE call: inception over ANY VSA object -> one hypervector. The seed can be a precomputed motif

## frame

- **`frame_budget_controller`** -- Build a FRAME-BUDGET CONTROLLER (holographic_framebudget) -- the one knob from a target FPS to concrete
- **`frame_budget_ms`** -- Convert a target frame rate to a per-frame millisecond BUDGET, minus a headroom fraction (a frame that
- **`frame_cache`** -- A tiered delta FrameCache for playback: `.put(frame, state)` stores each frame as a sparse DELTA vs
- **`frame_server`** -- Build a FRAME SERVER (holographic_framebudget) -- server-side real-time frame serving for front-end

## fuse

- **`fuse_depth`** -- FUSE classical depth cues (HAZE aerial-perspective + SHARPNESS depth-of-field) into one relative depth
- **`fuse_expression`** -- Fill 2: evaluate a five-op (bind/unbind/bundle/permute) expression tree in the FFT domain -- one
- **`fuse_rankings`** -- Reciprocal Rank Fusion (Cormack 2009): fuse several ranked id-lists into one by summing 1/(k+rank).
- **`fuse_record`** -- Fill 2 (spectral fusion): build a role/filler record -- bundle([bind(k_i, v_i)]) -- in ONE fused FFT

## game

- **`game_bus_host`** -- Host one node's slice of a sharded game world on the EXISTING message/distributed bus:
- **`game_shard`** -- An authoritative, deterministic fixed-tick GAME WORLD shard: player commands in, ticks,
- **`game_world`** -- A MASSIVE sharded game world: a lazy grid of game shards with deterministic cross-shard

## gather

- **`gather`** -- GATHER: sample a grid of any rank at continuous `points`, through the SAME kernel `scatter` deposits
- **`gather_field`** -- H2 -- apply a compiled rule to a baked field: ONE dot product, whatever N was.
- **`gather_from_grid`** -- The shared kernel GATHER = the READOUT: read a grid back at each point through the same kernel -- the
- **`gather_rule`** -- H2 -- compile N weighted lookups into ONE query vector: Q = sum_j w_j Z(u_j). A quadrature rule, a filter
- **`gather_samples`** -- H2 -- bake, compile a rule and gather in ONE call, from plain numbers to a plain number.

## generate

- **`generate`** -- Continue text from the chosen sequence schema. top_p<1.0 requests nucleus
- **`generate_gated`** -- Forecasting backlog (F5): confidence-gated generation -- generate a vector, then score how VALID it is
- **`generate_predictive`** -- Generate by anticipation: predict the next symbol, append, repeat.
- **`generate_procedure`** -- Generate a sample by running the B10 diffusion AS A VSA PROGRAM -- ITERATE [APPLY diffuse] from a noise
- **`generate_structure`** -- GENERATE a novel-but-valid COMPOSED structure by denoising from noise over the composition manifold
- **`generate_structured`** -- Generate while PROVING structure step by step: among the predictor's top
- **`generate_vector`** -- GENERATE a hypervector by denoising FROM PURE NOISE (B10) -- the cleanup attractor as a tiny
- **`generate_words`** -- Generate at the word level. topic_weight blends a topic-alignment pull

## gradient

- **`gradient_cache`** -- Package sparse anchors with cached values AND local Jacobians for first-order (Ward irradiance-
- **`gradient_cache_symbolic`** -- Build an irradiance/GI-style GradientCache with EXACT Jacobians from a symbolic field (SymPy) instead of
- **`gradient_uncertainty`** -- A cheap, deterministic uncertainty signal for a 2-D field: local gradient magnitude. Where a coarse

## graph

- **`graph_coloring`** -- Greedy graph colouring: each node takes the smallest colour no neighbour uses. DETERMINISTIC by
- **`graph_denoise`** -- Denoise / regularize a SET of vectors (a noisy codebook, an embedding, a value function) over its
- **`graph_namespace`** -- Sweep 3 local completion (fit-correct): a hierarchical namespace/navigation tree over labelled vectors
- **`graph_ring_order`** -- For hypervectors sampled on a 1-D ring, the recovered cyclic coordinate (holographic_simgraph, ARCH-3):
- **`graph_spectral_embedding`** -- Laplacian eigenmaps of a hypervector set (holographic_simgraph, ARCH-3): the `dims` lowest non-trivial

## guide

- **`guide_clamp_link`** -- Constraint builder for guide_structure: clamp the distance between state[i] and state[j] to at most
- **`guide_pin`** -- Constraint builder for guide_structure: pin state[index] to `value` (an IK end-effector target / a
- **`guide_snap`** -- Constraint builder for guide_structure: snap every state entry to its nearest value in `book` (the
- **`guide_structure`** -- Guide a `state` toward a goal by ITERATING A PROJECTION -- the level-generic form of IK / PBD / denoise

## harmonic

- **`harmonic_atom`** -- CONTEXT-CONDITIONED ATOM (holographic_harmonic, RT-VI) -- a polysemous atom whose decoded MEANING is a
- **`harmonic_dc`** -- The DC (degree-0), context-FREE meaning of a context-conditioned atom (holographic_harmonic, RT-VI) --
- **`harmonic_decode`** -- Read a context-conditioned atom (holographic_harmonic, RT-VI) at context angle `theta` -- the harmonic
- **`harmonic_fill`** -- Fill a CONTINUOUS field's holes by a Laplace solve: each hole relaxes to the mean of its four neighbours,

## holographic

- **`holographic_fog_volume`** -- Encode a volumetric DENSITY field (fog/atmosphere) as ONE hypervector via Fractional Power Encoding, and
- **`holographic_radiance_field`** -- Bake scene RADIANCE (colour leaving each surface point) into a TILED holographic field: space is split into
- **`holographic_value_head`** -- The creature's value/policy AS a pure-VSA program. Returns a HolographicValueHead (or, with

## hypervector

- **`hypervector`** -- The same encode, but returned as a first-class Hypervector (consolidation D1): the raw array plus its
- **`hypervector_layer`** -- **A hypervector used as an operator is ALWAYS the abelian ideal, and the algebra forbids anything else.**
- **`hypervector_to_grid`** -- Read an FPE field hypervector back onto a grid (the inverse of grid_to_hypervector). See

## identify

- **`identify_dynamics`** -- Identify MASS / MOMENTUM / dynamics from measurements (holographic_sysid),
- **`identify_element`** -- Identify the element(s) whose categorical fingerprint {category, state} best matches `props`, e.g.
- **`identify_level`** -- 'What am I looking at?' -- classify a CORPUS by which ladder operations pay on it, returning a

## image

- **`image_archive`** -- The DCT/Walsh-Hadamard plate archive (holographic_archive.HolographicArchive) as a mind faculty --
- **`image_classes`** -- Cluster a set of images into k visual classes with NO labels (holographic_vision.emergent_classes):
- **`image_colours`** -- The k most common colours in an image (holographic_vision): k-means++ clustering over a pixel sample --
- **`image_corners`** -- Top-n Harris corners of an image as (x, y) points, greedily spaced at least `min_dist` apart
- **`image_distance`** -- Inverse-rendering IR4: 1 - compare_images -- the objective the analysis-by-synthesis loop MINIMIZES
- **`image_edges`** -- Boolean edge map of an image (classic CV, holographic_vision): Sobel gradient magnitude thresholded at
- **`image_lines`** -- Dominant straight lines in an image by the classic Hough transform (holographic_vision): every edge
- **`image_signature`** -- One fixed-length feature vector describing an image (holographic_vision.describe): colour histogram +
- **`image_to_3d`** -- END-TO-END PHOTO-TO-3D from a single image (C1->C2->C3): estimate depth by shape-from-shading, unproject
- **`image_to_mesh`** -- END-TO-END image -> MESH: estimate depth by shape-from-shading, unproject to points, derive oriented

## is

- **`is_a`** -- Taxonomic membership over the absorbed encyclopedia: does `concept`
- **`is_affine_matrix`** -- Does the 4x4 `M` fix the plane at infinity -- is its bottom row [0,0,0,*]? A BOOLEAN, not a tolerance on
- **`is_deterministic`** -- Does `fn` return a bit-identical result for a repeated input? The gate a cache needs, and D1's
- **`is_diagonalisable`** -- Can ONE Fourier spectrum represent `transform`? Fit it on an encoded point, apply to another, return the
- **`is_fragile`** -- Is a measured claim FRAGILE? True if its spread is large relative to how far its mean sits above the
- **`is_manifold`** -- A fast structural GATE: does this point cloud form a single connected low-dimensional MANIFOLD, or is

## job

- **`job_cancel`** -- Stop a job for good (not resumable) at its next checkpoint boundary.
- **`job_list`** -- {job_id: status} for every job on the shared manager (this process's, plus any reopened from
- **`job_pause`** -- Ask a running job to pause at its next checkpoint boundary (waits for it to actually stop). Safe to
- **`job_result`** -- The finished job's result (for bake_cloud_job: the baked (grid,grid,grid) noise array). Raises if the
- **`job_resume`** -- Continue a paused job from where it left off (only the remaining buckets run).
- **`job_status`** -- {id, status: created/running/paused/done/cancelled/failed, progress in [0,1], done, total, error} for

## ladder

- **`ladder_forecast_calibrated`** -- Forecast the next value of a numeric `series` with the ladder predictor, wrapped in a CALIBRATED
- **`ladder_kit_report`** -- Report a ladder LEVEL's invariant-kit slots (from a climb_ladder tower) as WIRED, DECLARED-NEGATIVE, or
- **`ladder_predict`** -- Predict what comes NEXT after `history` using the ladder's learned HIERARCHICAL alphabet (the
- **`ladder_summary`** -- A compact human-readable summary of a climbed tower (from climb_ladder): per level its depth, alphabet

## learn

- **`learn`** -- Learn one labelled example NATIVELY -- the base learning verb the whole curriculum is built on. Perceives
- **`learn_chaos`** -- Learn a NONLINEAR dynamics operator -- the companion to `learn_dynamics`. Where
- **`learn_chunks`** -- Learn a CHUNK CODEBOOK from a symbol stream by iterated pair promotion (BPE -- Gage 1994; Sennrich et
- **`learn_cleanup`** -- Learn a cleanup's ATTRACTORS instead of storing them -- a LEARNED energy memory. Every cleanup
- **`learn_dictionary`** -- LANGUAGE CURRICULUM, layer 1 -- learn word MEANING from a dictionary,
- **`learn_dynamics`** -- Learn a fixed dynamics operator U so that state(t+1) ~ bind(U, state(t)) -- dynamics as an
- **`learn_encyclopedia`** -- LANGUAGE CURRICULUM, layer 3 -- learn RELATIONAL knowledge (an
- **`learn_hierarchical`** -- Absorb observations of a possibly-NESTED plan. Each observation is a
- **`learn_plan`** -- Store an ORDERED plan/recipe/protocol by name. Unlike absorb (which
- **`learn_procedure`** -- Store a named PROCEDURE -- an executable ACC->ACC recipe of VSA operations, assembled into
- **`learn_recipe_grammar`** -- Learn the sequence statistics of a set of valid recipes, so the next instruction can be
- **`learn_sequence`** -- Learn to continue a sequence.
- **`learn_sequences`** -- Absorb (sequence, label) pairs, KEEPING the raw ordered members per
- **`learn_vocabulary`** -- Bootstrap the mind's word meanings from the VENDORED DICTIONARY over a given vocabulary: builds the
- **`learn_word_generator`** -- Train a WORD-level context generator (holographic_generation): a word

## load

- **`load`** -- Reload a mind saved with save(); dispatches through the kernel's versioned loader.
- **`load_glb`** -- Load a .glb/.gltf into a LoadedMesh with its PBR materials (base colour / metallic-roughness / normal /
- **`load_obj`** -- Load a Wavefront .obj and its .mtl into a LoadedMesh (positions, UVs, normals, per-face material, and the
- **`load_skydata`** -- Load a sky observation saved by save_skydata (exact round-trip, no pickle). See
- **`load_texture_set`** -- Build one PBRMaterial from a folder of maps exported by Adobe Substance 3D Painter (or any tool):
- **`load_volume`** -- Load a 3-D density grid (.npy, or raw floats + dims) into (GridField, bounds) you can hand to

## machine

- **`machine_map`** -- THE SPEC SHEET: every hardware unit this engine has, as plain data -- {name, gpu_name, kind, module,
- **`machine_place`** -- Should this work go on a unit, or stay on the baseline? Pure arithmetic over a measured cost model:
- **`machine_place_unit`** -- `machine_place` on MEASURED numbers: look the unit's (setup, marginal) up in a spec sheet instead of
- **`machine_spec_sheet`** -- MEASURE the units on THIS machine, now, rather than trusting a comment: {unit: {setup_ns, marginal_ns,
- **`machine_unit`** -- One unit's full record, including the conditions under which it must NOT be used.
- **`machine_units`** -- The unit names, optionally filtered to 'compute' or 'memory'. The discovery door.

## make

- **`make_cloud`** -- Render a convincing volumetric CLOUD in ONE call and get back an (H,W,3) image in [0,1].
- **`make_incompressible`** -- Pressure-project a velocity field to divergence-free (the fluid solver's Helmholtz step, an FFT
- **`make_incompressible_3d`** -- Pressure-project a 3-D velocity field to divergence-free (the 3-D FFT Helmholtz solve). Returns
- **`make_mixture`** -- Create a Mixture -- the multi-channel matter model (smoke/dye/milk/oil-water are this with different
- **`make_observer`** -- Assemble a custom sensor from a wavelength grid + per-channel sensitivity curves ((nchan,nlam)).
- **`make_scene`** -- Query Interface (Phase 4): build a SCENE from a list of nested objects -- each object is encoded as a
- **`make_sky_axis`** -- One WCS-lite axis descriptor (world = crval + (pixel-crpix)*cdelt, crpix 0-based) for a sky cube. See
- **`make_skydata`** -- Assemble a sky observation: a data cube + one world axis per data dim (+ freeform meta). The container
- **`make_table`** -- Query Interface (Phase 1): ingest tabular data (a list of {column: value} dicts) into a VSA Table --
- **`make_uv_shell`** -- Build a UV SHELL (texture-carrying ENVELOPE): push every vertex OUTWARD along its normal by `offset`,

## manifold

- **`manifold_chart`** -- Flatten a CURVED hypervector manifold to a low-D coordinate chart -- the nonlinear extension of
- **`manifold_denoise`** -- Settle a (noisy) point ONTO a sample-defined manifold by looping a dense-Hopfield step (XDATA-2) --
- **`manifold_generate`** -- Generate a NOVEL-but-VALID sample on a sample-defined manifold by annealed diffusion (XDATA-2): from
- **`manifold_topology`** -- Name a point cloud's topology by persistent homology (holographic_topology, EXP-7): the Betti

## mantis

- **`mantis_falsecolor`** -- SEE WHAT THE MANTIS SEES: turn a mantis_view() reading into three human-viewable RGB images -- color
- **`mantis_receptors`** -- The mantis shrimp's ~12 spectral receptors (deep UV to far red) as an observer on the given wavelength
- **`mantis_view`** -- See a spectral-Stokes signal (...,nlam,4) as a mantis shrimp does: 12 spectral bands + linear +

## material

- **`material`** -- G2 -- a PBR material as a role-filler HRR record; textures as FPE functions over UV.
- **`material_catalog`** -- The whole render-material library grouped by class (diffuse/metal/wood/stone/glass/gem/emissive/biome/
- **`material_elemental`** -- A material's ELEMENTAL makeup and everything derived from it: {composition (element:count/ratio),
- **`material_from_vsa_record`** -- Recover a PBRMaterial from its hypervector record (unbind each channel role, decode the scalar).
- **`material_inclusions`** -- An IMPURITY/INCLUSION colour socket f(points)->(M,3): the `base` material everywhere except in
- **`material_info`** -- EVERYTHING the engine knows about a named material in one view: its RENDER appearance (PBR: base colour,
- **`material_layer`** -- Make one LAYER of a layered material: a `kind` ('base','diffuse','specular','reflection','coat','clearcoat')
- **`material_thermal`** -- Thermal properties of a named material -- {'density','specific_heat','thermal_conductivity'} in SI,
- **`material_to_mtl`** -- A PBRMaterial as an MTL block (the OBJ companion): modern PBR keywords (Pr/Pm/Ke) + legacy Kd/d/Ns so
- **`material_to_vsa_record`** -- Carry a PBRMaterial as ONE hypervector (each factor scalar-encoded, bound to its channel role, then
- **`material_units`** -- The units of the physical properties (density -> kg/m^3, youngs -> GPa, ...) -- so a returned value is

## materials

- **`materials`** -- The whole material roster: every material with which library it lives in, plus a summary (counts by render
- **`materials_by_category`** -- The physical materials in a category (metal / liquid / gas / polymer / ceramic / glass / mineral / stone /
- **`materials_from_mtl`** -- Parse an MTL file's text into a list of PBRMaterial. See holographic_materialio.materials_from_mtl.

## measure

- **`measure`** -- The VARIANCE HARNESS (holographic_measure) -- every headline number gets a mean, a spread, and a
- **`measure_area`** -- Modeling-app backlog (measurement + units): total surface area of a mesh, as a dimensioned [m^2]
- **`measure_bbox`** -- Modeling-app backlog: the axis-aligned bounding box of a mesh (extents + diagonal as [m] Quantities).
- **`measure_distance`** -- Modeling-app backlog: the length between two points, as a dimensioned [m] Quantity (convert with
- **`measure_report`** -- Format a stats dict from `measure` as 'name: mean +/- std (95% CI [lo, hi], n)', flagging FRAGILE when
- **`measure_volume`** -- Modeling-app backlog: enclosed volume of a CLOSED mesh (divergence theorem), a [m^3] Quantity. See

## mesh

- **`mesh_auto_crease`** -- AUTO-CREASE: build a crease map for mesh_catmull_clark by tagging every edge whose DIHEDRAL angle exceeds
- **`mesh_auto_seam`** -- AUTO-MARK SEAMS for UV unwrapping (holographic_meshseam): choose which edges to cut WITHOUT naming a path.
- **`mesh_bevel_vertex`** -- BEVEL a corner (holographic_meshverbs2, FWD-7 remainder): pull each edge incident to `vertex` back toward
- **`mesh_box`** -- An axis-aligned box as a quad Mesh (holographic_mesh, FWD-1): the canonical first explicit mesh
- **`mesh_bridge`** -- BRIDGE two edge loops (holographic_meshverbs2, FWD-7 remainder): join two equal-length ordered vertex
- **`mesh_catmull_clark`** -- CATMULL-CLARK subdivide (holographic_meshsubdiv.catmull_clark) -- THE quad box-modelling subdivision
- **`mesh_cluster_decimate`** -- PARALLEL decimation by vertex clustering (holographic_meshqem, Rossignac-Borrel / Lindstrom) -- the O(n)
- **`mesh_cluster_lod_chain`** -- A fast PARALLEL level-of-detail chain (holographic_lod) for an IMPORTED mesh: vertex-cluster
- **`mesh_collapse_edge`** -- Merge edge endpoint `remove` into `keep` (holographic_eulerops, FWD-7): the decimation/LOD
- **`mesh_connected_components`** -- The number of connected components of a mesh (holographic_route, ARCH-7; flood fill over edge adjacency).
- **`mesh_crease_edges`** -- Build a CREASE map for mesh_catmull_clark from a list of edges: {(vi,vj): sharpness}. `edges` is a list
- **`mesh_creases`** -- The sharp edges of a mesh (holographic_meshcurvature, FWD-6): interior edges whose DIHEDRAL angle
- **`mesh_csg`** -- Boolean of two solids by ROUTING through the SDF (holographic_route, ARCH-7's flagship): `operation` in
- **`mesh_curvature`** -- Per-vertex curvature of a mesh (holographic_meshcurvature, FWD-6). kind='mean' -> |H| via the
- **`mesh_curvature_confidence`** -- A per-vertex confidence in [0,1] for the curvature estimate (holographic_meshcurvature, FWD-6), from
- **`mesh_cut_seam`** -- Cut a mesh open along a SEAM (holographic_meshseam, ARCH-4): given `seam` (an ordered list of vertex
- **`mesh_dissolve_vertex`** -- DISSOLVE a vertex (holographic_meshverbs, FWD-7; the Euler KEV verb): remove `vertex` and its incident
- **`mesh_euler`** -- The combinatorial well-formedness signature of a Mesh (holographic_mesh, FWD-1): vertices,
- **`mesh_extrude`** -- EXTRUDE a face (holographic_meshverbs, FWD-7): lift face `face_index` along its outward normal by
- **`mesh_face_counts`** -- {3: triangles, 4: quads, 5: n-gons} -- the face-standard summary. See holographic_meshpoly.
- **`mesh_face_type`** -- Convert ANY triangle mesh's face standard (holographic_meshpoly) WITHOUT moving vertices (so stable
- **`mesh_field_lod`** -- FIELD-NATIVE level-of-detail for an IMPORTED mesh (the decomposition closure): convert it to a full SDF
- **`mesh_fill_holes`** -- FILL open holes (boundary loops) of a mesh with faces (holographic_meshverbs2), returning a closed-up Mesh.
- **`mesh_flip_edge`** -- Rotate the edge shared by two triangles (holographic_eulerops, FWD-7): remove edge a-b, add edge
- **`mesh_from_gltf`** -- Parse binary glTF (.glb) bytes back into a Mesh (holographic_gltf, FWD-2): the inverse of
- **`mesh_from_sdf`** -- Extract a MESH from an implicit field (holographic_meshbridge, FWD-11; SDF -> mesh): sample the scalar
- **`mesh_geodesic`** -- Single-source surface geodesic distance (holographic_meshgeodesic, FWD-5): shortest path ALONG mesh
- **`mesh_grid`** -- A flat subdivided plane as an (nx by ny) quad grid (holographic_mesh, FWD-1): an OPEN mesh
- **`mesh_inset`** -- INSET a face (holographic_meshverbs, FWD-7): shrink face `face_index` toward its centroid by `ratio`,
- **`mesh_is_oriented`** -- Is every DIRECTED edge traversed exactly once? The property a half-edge structure, a normal, and any
- **`mesh_limit_surface`** -- The Loop LIMIT surface in closed form: where infinite subdivision would put every vertex, plus the EXACT
- **`mesh_lod_chain`** -- Build a level-of-detail CHAIN (holographic_lod): QEM-decimate `mesh` to successively coarser levels at
- **`mesh_loop_cut`** -- LOOP-CUT: insert an edge loop (holographic_meshverbs2, FWD-7 remainder): trace the perpendicular loop of
- **`mesh_lscm`** -- LEAST-SQUARES CONFORMAL MAP (Levy et al., SIGGRAPH 2002): the angle-preserving unwrap, as ONE linear
- **`mesh_make_manifold`** -- Make a mesh MANIFOLD by splitting non-manifold vertices into their connected umbrellas
- **`mesh_pack_uv`** -- PACK UV ISLANDS (holographic_meshuv): unwrap each connected component (UV island) of `mesh` SEPARATELY,
- **`mesh_point_distance`** -- Distance from query points (N,3) to a mesh, ACCELERATED by a vectorized spatial grid that culls the work
- **`mesh_poke`** -- POKE polygon face `f_index` (holographic_eulerops, FWD-7): add a vertex at the face centroid (pushed out
- **`mesh_qem_decimate`** -- QEM (quadric error metric) DECIMATION (holographic_meshqem, Garland-Heckbert): greedily collapse the
- **`mesh_repair`** -- REPAIR a raw mesh by composing the standard cleanup ops (holographic_meshtools.mesh_repair): WELD
- **`mesh_report`** -- MESH REPORT, dispatching on the input (this method was DEFINED TWICE after a branch merge -- the
- **`mesh_rip_vertex`** -- RIP a shared vertex apart (holographic_eulerops): give every face incident to `vertex` its OWN copy at the
- **`mesh_sample_field`** -- Trilinearly sample a banded SDF (from mesh_to_field) at query points (N,3) -> (N,) SIGNED distances; take
- **`mesh_select_lod`** -- Choose a level of detail by SCREEN-SPACE ERROR (holographic_lod): the index of the coarsest level in
- **`mesh_selection`** -- A sub-object MESH SELECTION (holographic_meshselect) -- a persistent set of VERTS / EDGES / FACES with a
- **`mesh_shortest_seam`** -- A seam path between two vertices (holographic_meshseam, ARCH-4): the shortest edge path from `a` to `b`
- **`mesh_smooth`** -- Taubin lambda|mu no-shrink mesh smoothing / denoising (holographic_meshsmooth, FWD-4): the shipped
- **`mesh_soft_selection`** -- A soft-selection falloff in [0,1] per vertex by GEODESIC distance from `source` within `radius`
- **`mesh_split_edge`** -- Insert a midpoint vertex on edge {a,b}, splitting the incident triangle(s) (holographic_eulerops,
- **`mesh_split_face`** -- Cut polygon face `f_index` with a diagonal between its i-th and j-th corners (holographic_eulerops,
- **`mesh_split_vertices`** -- SPLIT every vertex per-face (holographic_eulerops): give each face its own private copies of its corners,
- **`mesh_stable_uv`** -- UVs that are a deterministic function of WORLD POSITION, so they DON'T move under local edits -- the
- **`mesh_subdivide`** -- Loop-SUBDIVIDE a triangle mesh (holographic_meshsubdiv, FWD-8): refine each triangle into four and
- **`mesh_surface_deviation`** -- A decimation QUALITY metric (holographic_meshqem): (mean, max) point-to-surface distance from mesh_a's
- **`mesh_symmetrize`** -- SYMMETRIZE a mesh across the `axis`=const `plane` (holographic_meshtools): KEEP the half on `side` (+1 =
- **`mesh_tetrahedron`** -- A regular tetrahedron as 4 triangles (holographic_mesh, FWD-1) -- the smallest closed manifold,
- **`mesh_to_field`** -- The mesh -> FIELD direction (holographic_meshbridge.mesh_distance_grid): decompose a mesh into a SIGNED
- **`mesh_to_field_vector`** -- FS-5: carry a surface as a SINGLE hypervector (edit = bind). Samples the mesh's signed distance on a
- **`mesh_to_gltf`** -- Serialise a Mesh to single-file binary glTF (.glb) bytes (holographic_gltf, FWD-2): the boundary
- **`mesh_to_sdf`** -- Signed distance from a MESH at query points (holographic_meshbridge, FWD-11; mesh -> implicit): the
- **`mesh_to_sdf_grid`** -- Convert an imported mesh into a FULL, re-marchable signed distance field
- **`mesh_to_softbody`** -- Turn ANY mesh into a simulatable SoftBody (vertices -> particles, edges -> distance constraints) so a
- **`mesh_to_stl`** -- CAD EXPORT (K7): an ASCII STL string for a mesh (tris/quads/ngons; per-facet normals from the winding).
- **`mesh_triangulate`** -- EAR-CLIP every face of `mesh` into triangles (holographic_meshverbs2), returning a new all-triangle Mesh.
- **`mesh_uv_angle_distortion`** -- The metric LSCM optimises: per-face quasi-conformal ratio sigma1/sigma2. Returns {mean, median, max,
- **`mesh_uv_area_distortion`** -- The log-spread of (UV face area / 3-D face area); 0 = area-preserving. The price a conformal map pays.
- **`mesh_uv_distortion`** -- The per-edge STRETCH distortion of a UV map (holographic_meshuv, FWD-3): spread of (UV edge length /
- **`mesh_uv_report`** -- Every chart on every metric -- {method: {angle, area, stretch}} -- so nobody compares two charts on a
- **`mesh_uv_unwrap`** -- UV-unwrap a (disk-topology) mesh to 2-D coordinates (holographic_meshuv, FWD-3): classical MDS of the
- **`mesh_volume`** -- The enclosed volume of a closed mesh (holographic_route, ARCH-7; divergence theorem over outward-wound
- **`mesh_weld`** -- WELD vertices closer than `tol` into one (holographic_meshtools.merge_by_distance): snap to a tol grid,
- **`mesh_winding_number`** -- Generalised WINDING NUMBER of each query point w.r.t. a triangle mesh: ~1 inside a closed surface, ~0

## navigate

- **`navigate_cost_field`** -- NAVIGATE a known N-D COST FIELD: discretize it to a grid, weight edges by the field, and return the
- **`navigate_field`** -- Climb the attractor field from a query toward an attractor (iterated, decaying-step deflection),
- **`navigate_scene`** -- Route an agent through a LIVE SCENE using its own SDF as the cost field: inside geometry is impassable,

## nbody

- **`nbody_accel`** -- Softened Newtonian acceleration on each body from all others (the O(N^2) direct sum). See
- **`nbody_energy`** -- Total mechanical energy KE + PE of an N-body system -- the quantity a good integrator keeps bounded. See
- **`nbody_simulate`** -- Integrate an N-BODY gravity system forward `steps` velocity-Verlet steps (symplectic -> energy stays
- **`nbody_step`** -- One velocity-Verlet step of N-body gravity -> (positions, velocities, accel). See holographic_nbody.nbody_step.

## nebula

- **`nebula_column`** -- Column density: sum a nebula volume along one axis -> a 2-D image (looking through the cloud), the cheap
- **`nebula_field_fn`** -- Wrap a nebula volume as the callable points(N,3)->density the volume renderer marches (trilinear). Drop a
- **`nebula_volume`** -- Build a 3-D NEBULA density volume (res^3, [0,1]) -- turbulent gas/dust with wispy filaments and dark

## nurbs

- **`nurbs_circle`** -- A NURBS CIRCLE -- the canonical proof that NURBS represent conics EXACTLY (every point at `radius` to
- **`nurbs_curve`** -- Evaluate a NURBS (rational B-spline) CURVE: `control` (k,dim) with per-point `weights` (default 1 -> a
- **`nurbs_surface`** -- Evaluate a NURBS SURFACE (tensor-product rational B-spline): `control_grid` (ku,kv,3) control net with
- **`nurbs_surface_mesh`** -- Tessellate a NURBS surface into a triangle MESH (vertices, faces) -- the bridge from a CAD patch to the

## phase

- **`phase_fold`** -- Fold a series on `period` -> (phase in [0,1), values) sorted by phase. Coherent on the true period,
- **`phase_morph`** -- Morph between two FHRR phasor vectors in the PHASE domain (phase shift = motion) -- interpolate each
- **`phase_randomize`** -- A PHASE-RANDOMIZED surrogate of a 1-D signal: same power spectrum (same autocorrelation) as `x` but
- **`phase_state`** -- A parcel of `material` with mass across solid/liquid/gas at one temperature: `.add_heat(Q)` warms it and

## plan

- **`plan`** -- Bake one CORRIDOR -- a short executable route to the next decision point -- on the directed
- **`plan_pipeline_bakes`** -- Compile a pipeline (PW2) then decide which of its stages to BAKE vs COMPUTE over `frames` frames (PW3) --
- **`plan_render`** -- The DECISION LAYER of the adaptive pipeline, on its own: given a scene and workload, return the plan (bake
- **`plan_route`** -- Bake a WHOLE arbitrarily-long route in one call, by chaining cap-sized corridors and re-anchoring
- **`plan_shape`** -- Build the SHAPE (the decode key) for a contingency plan: the action and scope value codebooks and a
- **`plan_stage_execution`** -- Decide bake-vs-compute PER pipeline stage (PW3), extending adaptive.plan_render's break-even to the
- **`plan_waves`** -- Physics backlog (#5, the AdaptiveSolver): the DECISION LAYER for the ocean stack -- per tile, pick the
- **`plan_write_waves`** -- Schedule database write batches into key-disjoint waves (X10): the single-writer lock serialises writers

## postfx

- **`postfx_apply_transfer`** -- Evaluate a composed transfer: ONE FFT pair per channel, whatever the run length was. Measured on a
- **`postfx_chain`** -- Build a post-processing PostChain program from (name, params) steps, e.g.
- **`postfx_fusable_runs`** -- Split a post-chain's steps into maximal [(is_fused, [steps])] runs. MEASURED: the shipped chains have NO
- **`postfx_fuse_transfers`** -- Compose a RUN of linear, shift-invariant post-effects (denoise, sharpen) into ONE transfer -- the

## quantum

- **`quantum_dot_well`** -- A narrow Gaussian potential well (the quantum dot) -- hand it to a QuantumField.set_potential. A negative
- **`quantum_field`** -- A COMPLEX wavefunction psi on a grid -- the central quantum object. `.gaussian_packet(center, sigma, k0)`
- **`quantum_solenoid_A`** -- The azimuthal vector potential of a thin solenoid carrying `flux` -- thread it through a ring with
- **`quantum_solver`** -- A split-operator (split-step Fourier) integrator for the time-dependent Schrodinger equation on a
- **`quantum_transmission`** -- MEASURE the fraction of a packet (carrier k0) that crosses a cut plane past an optional dot potential --
- **`quantum_two_slit`** -- Build a two-slit interferometer: a high-potential wall with two openings. Returns (QuantumField, V).
- **`quantum_velocity`** -- The probability VELOCITY field v = j/|psi|^2 -- hand it straight to advect_field to carry glowing tracers

## query

- **`query`** -- Query Interface (Phases 2-3): run a small SQL subset (SELECT/FROM/WHERE/ORDER BY/LIMIT) over a VSA
- **`query_scene`** -- Query Interface (Phase 4): run a GraphQL query over a scene -- 'ask for exactly the nested fields you
- **`query_scene_slot`** -- Read object `slot` back OUT of the bundled scene hypervector (unbind its role, then decode each attribute

## ray

- **`ray_mesh_intersect`** -- RAY-VS-MESH picking (holographic_raypick) -- cast a ray at a mesh {vertices, faces} and return the
- **`ray_path_index`** -- Build a BIDIRECTIONAL ray<->object index for a scene: which objects each camera ray TOUCHED along its path
- **`ray_pencil`** -- Emit a ray's PERPENDICULAR FRAME (centre + 4 marginal rays offset +-eps) and transport it through a
- **`ray_sdf_intersect`** -- RAY-VS-SDF picking (holographic_raypick) -- sphere-trace a ray into an SDF (any sdf_fn(pt)->distance) and

## read

- **`read`** -- Pre-learn word co-occurrence so text perceptions carry meaning.
- **`read_irradiance`** -- Read an irradiance cache at query points by inverse-distance interpolation of the k nearest cached
- **`read_role`** -- Decode one role's filler from a LEARNED class -- unbind the role from
- **`read_wav`** -- Read a PCM WAV file -> (samples in [-1,1] mono, sample_rate). The front door for driving acoustics/

## realize

- **`realize`** -- Replay a StructureRecipe to its output vector(s) -- the single realize path for any structure.
- **`realize_recipe_fused`** -- Fill 4 integration at Layer 4: realize a StructureRecipe's outputs through the SCHEDULER, fusing its
- **`realize_structure`** -- Compile a structure-description spec (ISA-7) and materialize its vector -- bit-exact for a given

## recall

- **`recall`** -- Nearest stored individual. The index does an exact scan until the store is
- **`recall_and_apply`** -- Recall the procedure that performs a demonstrated (input -> output) transform, then APPLY it
- **`recall_calibrated`** -- CORE calibrated recall: the nearest STORED INDIVIDUAL plus an honest false-alarm probability --
- **`recall_procedure`** -- Goal-addressable recall over the procedure library: given ONE (input -> output) example,

## recipe

- **`recipe_commute_bind`** -- Edit a recipe (holographic_recipeops, ARCH-1): swap the two arguments of the bind at `handle`,
- **`recipe_reorder_members`** -- Edit a recipe (holographic_recipeops, ARCH-1): permute the members of the bundle/superpose at `handle`
- **`recipe_substitute_atom`** -- Edit a recipe (holographic_recipeops, ARCH-1): rename the atom leaf at `handle` -- the recipe analogue

## recognize

- **`recognize`** -- CORE calibrated recognition. Like classify, but returns (label, similarity, pvalue): the
- **`recognize_batch`** -- Recognise a BATCH honestly: classify each query, then control the FALSE-DISCOVERY RATE across
- **`recognize_elements`** -- Instancing GENERALISED: two elements share a class when they are the same thing modulo the family's

## region

- **`region_boolean_area`** -- 2D REGION BOOLEAN (K4): the area of union/difference/intersection of two closed polygonal regions, by
- **`region_field`** -- Compose a LABELLED REGION FIELD: a set of boundaries (SDFs), each tagging the points inside it with how to
- **`region_membership`** -- A predicate (x,y)->bool for the boolean op {union,difference,intersection} of two 2D regions -- the

## register

- **`register_apply_handler`** -- Make any unary acc->acc faculty callable from a HoloMachine program as `APPLY <name>` -- INCLUDING
- **`register_capability`** -- Register a capability in the catalog so future `find_capability` calls surface it (backlog C1: as each
- **`register_code_idiom`** -- Grow the idiom catalog (C6): register a purpose-recognizer by EXAMPLE -- the example function's
- **`register_composition_primitive`** -- Grow C6's COMPOSITION recognizer: register a primitive by its distance EXPRESSION (e.g. a sphere's
- **`register_geometry_form`** -- Grow C3's controlled vocabulary: register a parametric SDF form whose `body_fn(params)` returns kernel

## render

- **`render_adaptive`** -- ONE render call that ADAPTS -- it looks at the scene and the workload and picks the methods itself instead of
- **`render_auto`** -- AUTO-CALIBRATING render -- one quality knob, no per-scene spp or denoise tuning. It wires together
- **`render_baked`** -- Relight a BakedScene (from bake_scene) -- shade every pixel from its precomputed transfer, no tracing. Every
- **`render_channels`** -- Inverse-rendering IR14: render selectable, separate AOV channels (depth/normal/position/mask G-buffer,
- **`render_checkerboard`** -- Inverse-rendering IR13: checkerboard/sparse render -- shade only ~50% of the pixels (a 2x2 pattern) and
- **`render_demodulated_upscale`** -- M5 -- render a HIGH-resolution frame at LOW-resolution lighting cost. Render the expensive lighting at
- **`render_dispatch`** -- RENDER by dispatching each hit to its best method and get a RELIGHT handle -- the pipeline form of "collapse
- **`render_frame_delta`** -- The pixel-streaming primitive: return only the `tile`x`tile` image blocks that CHANGED between two
- **`render_graph`** -- An ORCHESTRATOR that prepares the CMP1-CMP4 graphs into a render-ready scene as pipeline stages. Register
- **`render_hair`** -- RENDER HAIR (H4/H5/H6): project each strand's smoothed centerline and shade its segments by their
- **`render_material`** -- A PHYSICALLY-PLAUSIBLE render material from the library (holographic_matlib) as a first-class
- **`render_mesh`** -- Rasterise a mesh to an (H,W,3) RGB image with a z-buffer and Lambert shading (frustum + back-face
- **`render_pipeline`** -- Render/Sim Pipeline (Phases 1-2): build a configured, validated render+sim pipeline. `preset` is
- **`render_scene`** -- Render composed attribute tags to an actual RGB image via the scene renderer.
- **`render_scene_description`** -- The full text -> 3-D pipeline in one call: parse the description and render. quality='fast' uses the
- **`render_scene_document`** -- Render the canonical SCENE DOCUMENT (holographic_scene_doc.Scene) -- the 'a modeling app builds a
- **`render_sdf`** -- Field-native SDF renderer: sphere-trace primary rays, then shade hits with Lambert direct light gated
- **`render_sdf_fast`** -- Render an analytic SDF (given as a symbolic expression) with the fully-JIT'd renderer: the whole march --
- **`render_session`** -- Open a RENDER SESSION over one scene -- the object that ties the renderers together so a preview and a
- **`render_surface`** -- Render an SDF scene resolving every material channel PER HIT from its socket -- so a procedural pattern on
- **`render_textured`** -- Render a SemanticScene with COMPOSED textures/materials painted onto its objects -- the composability stack
- **`render_volume`** -- Volumetrically render a density FIELD (smoke/fire/water/particles) by marching camera rays and

## replay

- **`replay_margin`** -- Replay a query stream against a margin and count what it would cost: {hits, rebuilds, hit_rate, margin}.
- **`replay_margin_error`** -- Replay a query stream against a margin and measure what the cache would have SERVED against what was TRUE:
- **`replay_physics_trace`** -- Regenerate a full trace from base + events, BIT-IDENTICALLY (measured max|diff| exactly 0.0). Between

## resolve

- **`resolve_capability_uri`** -- Resolve a bare capability name or a partial path to the FULL capability URI(s) that match
- **`resolve_override`** -- Modeling-app feature layer: resolve a render property for an object -- its own override, else its
- **`resolve_param`** -- Resolve any parameter (constant / map / field / socket) to concrete values at `points`. See holographic_param.
- **`resolve_scenario`** -- Turn a physical description ('a block of wood floating in water', 'a steel ball sinking in oil') into a
- **`resolve_swept_collision`** -- Positional CCD for a PBD-style solver: a node that MOVED from X_prev to X must not have crossed the

## rm

- **`rm_peak`** -- Reduce a Faraday spectrum (or image cube of them) to its brightest source: {rm, polarized_intensity,
- **`rm_phi_grid`** -- Build a sensible grid of Faraday depths to evaluate, derived from the wavelength^2 sampling itself
- **`rm_resolution`** -- Faraday-depth resolution (FWHM, rad/m^2) from the wavelength^2 coverage: 2*sqrt(3)/span. Wider
- **`rm_synthesis`** -- Rotation-measure synthesis: the Faraday-depth spectrum F(phi) of polarized light vs wavelength^2

## route

- **`route`** -- AGENT-FRIENDLY decision node: when one skill clearly wins, returns {'decision':'act', 'skill':..., 'call'}
- **`route_repair`** -- Route a mesh to the MINIMAL repair its defect needs instead of always running the full pipeline:
- **`route_representation`** -- The routing POLICY (holographic_route, ARCH-7): the representation whose capability set supports
- **`route_semantic`** -- N28 -- route a request to the right MODULE by cosine in nomic's embedding space, not token
- **`route_structured`** -- Route a REQUEST to modules by holographic role-STRUCTURE, not a bag-of-words mean: parse request

## run

- **`run_analysis_pipeline`** -- Run the standard automatic-analysis pipeline -- a VSA PROGRAM -- over a 1-D signal and return the
- **`run_command`** -- Run an allowlisted external program: {stdout, stderr, returncode, ok}. Raises if `name` is not on the
- **`run_game_shard`** -- JSON-in/JSON-out one-shot game-world run: feed commands (+ optional saved state), get the
- **`run_game_world`** -- JSON one-shot over the sharded world: spawn/drive entities, get per-tick world digests,
- **`run_pipeline`** -- Run a pipeline for `cfg`, compiling its plan on the first frame and reusing it thereafter (PW1/PW2) -- the
- **`run_procedure`** -- Execute a procedure and return (accumulator, trace). `name_or_program` is the name of a
- **`run_procedure_batch`** -- Run a straight-line procedure over a BATCH of accumulators (N, D) in ONE interpret pass -- the
- **`run_simulation`** -- Build a known solver, run it `steps` times through the shared loop, and return its final field grid --
- **`run_task`** -- Run `fn(...)` as a named task on this mind's bus: publishes '<name>.start' then '<name>.done' (with a small
- **`run_waves`** -- Schedule CONFLICTING work into lock-free WAVES: two items in one wave never touch a shared resource, so
- **`run_with_auto_cleanup`** -- Run a vector 'program' (a list of vector->vector steps) under an error-correction policy (ISA-8),

## sample

- **`sample_directional`** -- Sweep 3 item 8: reconstruct a directional function from its spherical-harmonic coefficients at `dirs`
- **`sample_field`** -- Read a grid field at continuous particle positions (bilinear, periodic) -- how particles feel a
- **`sample_field_3d`** -- Read a 3-D grid field at continuous positions (N,3), trilinear+periodic -- how a softbody/particle
- **`sample_from`** -- Draw one symbol from a {symbol: weight} distribution with temperature + optional nucleus (top-p) --
- **`sample_imf`** -- Draw `n` stellar masses (solar units) from a Salpeter initial mass function (p(m)~m^-alpha, alpha=2.35).
- **`sample_instruction`** -- SAMPLE the next full instruction (the GENERATION dual of complete_instruction). Same learned
- **`sample_recipe`** -- Generate a whole recipe by SAMPLING the learned grammar step by step (the non-limit-cycling
- **`sample_texture`** -- Sample a texture graph at a UV/point -> a value (a scalar, or an rgb colour if the graph produces one).

## save

- **`save`** -- Persist the learned mind to `path` (.npz) via the kernel save. The default quant='auto' picks
- **`save_render`** -- Write a render (an (H,W,3) image in [0,1]), routed by extension: .png via the stdlib encoder
- **`save_skydata`** -- Persist a sky observation deterministically (JSON header + .npy cube in one .npz, no pickle). See

## scan

- **`scan`** -- Scan MANY candidate channels with the two disciplines a large search needs AT ONCE -- Siemion's
- **`scan_exact`** -- The PREFIX SUM, bit-identical however the work is blocked. A blocked FLOAT scan -- what every parallel
- **`scan_exact_blocked`** -- `scan_exact` computed the way a GPU or a farm would -- a cumsum per block plus the exclusive prefix of

## scatter

- **`scatter`** -- SCATTER = the BUNDLE: deposit each point's value onto a grid of any RANK through a kernel. `points`
- **`scatter_exact`** -- `scatter`, PERMUTATION-INVARIANT: the grid is bit-identical however the points are ordered or split.
- **`scatter_surface`** -- Scatter instances (grass, rocks, barnacles...) onto ANY surface -- a scatter LAYER that emits geometry
- **`scatter_to_field`** -- The adjoint of sample_field: imprint per-particle values onto a grid (bilinear, periodic) -- e.g. a
- **`scatter_to_field_3d`** -- The exact adjoint of sample_field_3d: imprint per-node values onto a 3-D grid (trilinear, periodic)
- **`scatter_to_grid`** -- The shared kernel SCATTER = a BUNDLE: deposit each point's value onto a grid through a kernel (bilinear

## scene

- **`scene`** -- The mind's own scene coder (compose/decompose visual attribute scenes), built
- **`scene_compose_transforms`** -- Compose 4x4 transforms (the product M0 @ M1 @ ..., parent then child) for scene_graph nodes.
- **`scene_control_spec`** -- Turn a control phrase ('control the ball size and how metallic it is') into UI control descriptors
- **`scene_cost`** -- Estimate the per-ray evaluation COST of an SDF scene (W2) -- an ALU/machine-model annotation for
- **`scene_dedup_saving`** -- Measure the content-addressed dedup saving across a set of scenes (holographic_scenedelta): {'naive',
- **`scene_delta`** -- The component DIFF between two scenes (holographic_scenedelta): {'added', 'removed'} content-hashed
- **`scene_flatten`** -- The GEOMETRY view of a scene graph (holographic_scenegraph): instance every leaf mesh through its
- **`scene_from_image`** -- BUILD A SCENE FROM A PHOTO (machine-initialised, not hand-authored): segment the image into regions,
- **`scene_graph`** -- Build a SCENE-GRAPH node (holographic_scenegraph): a 4x4 `transform`, an optional leaf `mesh`, optional
- **`scene_hypothesis`** -- Inverse-rendering IR3: an archetype-level scene READING of an image -- dominant palette, the horizon row
- **`scene_rotation`** -- A 4x4 rotation transform (Rodrigues, radians) for scene_graph nodes.
- **`scene_scaling`** -- A 4x4 scale transform (uniform scalar or per-axis length-3) for scene_graph nodes.
- **`scene_to_recipe`** -- The STRUCTURE view of a scene graph (holographic_scenegraph): encode the graph as a StructureRecipe --
- **`scene_to_render`** -- Flatten a Scene document to (sdf, material_fn) for the path tracer, without rendering -- the bridge
- **`scene_translation`** -- A 4x4 translation transform for scene_graph nodes (holographic_scenegraph).

## sdf

- **`sdf_collision_projection`** -- A collision PROJECTION callable for project_onto_constraints -- so 'stay outside this surface' is just one
- **`sdf_curvature`** -- MEAN CURVATURE of an SDF surface at `points` (W13) -- the field Laplacian (div of the unit gradient).
- **`sdf_dialect`** -- Emit the SDF TREE's own `map(p) -> distance` in `wgsl` | `glsl` | `c_f64` | `c_f32` -- so the browser
- **`sdf_emit_coverage`** -- Which SDF node kinds the dialect emitter handles and which it refuses. `emitted + refused == every kind`
- **`sdf_extrude`** -- EXTRUDE a 2-D SDF into a 3-D prism along Z (W10, iq's opExtrusion) -- a logo becomes a badge, a gear
- **`sdf_from_points`** -- A signed distance grid from an ORIENTED point cloud: distance to the nearest sample, signed by that
- **`sdf_object`** -- S2 -- a procedurally generated 3D OBJECT as an SDF tree, from a seed (the demoscene seed->world).
- **`sdf_offset`** -- The SPECULATIVE CONTACT MARGIN, and it costs one subtraction: enlarging a collider by `margin` is just
- **`sdf_parse`** -- S1 -- parse a compact SDF DSL string back into an SDF tree -- the INPUT side of shader I/O.
- **`sdf_render`** -- S1 -- march an SDF tree to a triangle Mesh through the engine's existing marching bridge.
- **`sdf_revolve`** -- REVOLVE a 2-D SDF around the Y axis into a solid of revolution (W10, a lathe) -- a vase, a bottle, a
- **`sdf_scene`** -- Build an SDF SCENE from parts (holographic_sdfscene) -- 'a scene is a set of SDF parts'. Pass a list of
- **`sdf_shader`** -- S1 -- emit a complete Shadertoy-ready GLSL fragment shader (map() + raymarch + normals + light)
- **`sdf_surface_points`** -- Sample points that lie ON an SDF's surface (random points + one Newton step onto the zero level, kept where
- **`sdf_to_mesh`** -- FRACTAL / SDF -> MESH, the one-liner (holographic bridge). Marches an SDF OBJECT (from fold_fractal /
- **`sdf_validate_c`** -- Compile the emitted C `map()` with `cc`, RUN it, and compare to the Python `_eval`. MEASURED on a compound

## select

- **`select_boundary_loops`** -- Select the OPEN BOUNDARY edges of a mesh (holographic_meshselect) -- the edges used by exactly one face
- **`select_edge_loop`** -- Select the EDGE LOOP through `seed_edge` (holographic_meshselect) -- the ring of edges continuing
- **`select_face_ring`** -- Select the FACE RING from `seed_face` (holographic_meshselect) -- the band of quads a loop cut runs
- **`select_in_box`** -- REGION SELECT (holographic_meshselect) -- select every element inside the axis-aligned box [lo,hi], the
- **`select_objects`** -- Modeling-app feature layer: select object handles from a Scene by exact predicates (name/material/tag/
- **`select_symmetric`** -- SYMMETRY SELECTION (holographic_meshselect) -- add a selection's mirror-image elements across a world

## set

- **`set_file_root`** -- Scope all file_* operations to `root` (a directory). Every subsequent path is resolved inside it and
- **`set_generator`** -- Configure the generative diffusion that APPLY diffuse runs as a PROGRAM. After this,
- **`set_inverse_problem`** -- Configure the inverse problem that APPLY datafit / APPLY denoise solve as a PROGRAM. After this,
- **`set_matmul`** -- Configure the matrix used by APPLY matmul: that opcode then does ACC := W @ ACC, carried by the
- **`set_override`** -- Modeling-app feature layer: bind a render override on an object (undoable). See

## skin

- **`skin_bind_weights`** -- AUTO-SKIN BINDING (holographic_meshskin) -- compute per-vertex bone weights from bone anchor points, the
- **`skin_mesh`** -- Linear-blend-SKIN a mesh (holographic_meshskin, FWD-9): deform each vertex as the weighted combination
- **`skin_skeleton`** -- SKIN A SKELETON (B-Mesh): wrap a stick figure -- verts (n,3), edges [(i,j)...], per-vertex radii (n,) --

## sky

- **`sky_dome`** -- HDRI sky dome: the environment radiance from `directions`. With `env` (an equirectangular (H,W,3)
- **`sky_lambda2`** -- The lambda^2 (m^2) vector of a cube's spectral axis -- the input Faraday RM synthesis wants (frequency
- **`sky_pix_to_world`** -- Pixel -> world coordinate on one sky axis (linear). See holographic_skydata.pix_to_world.
- **`sky_stokes_cube`** -- Reshape a sky observation into (...,nchan,4) -- spatial, then spectral channel, then Stokes -- ready for
- **`sky_world_coords`** -- The world-coordinate array (RA/Dec/freq...) along one axis of a sky cube, by index or name. See
- **`sky_world_to_pix`** -- World -> pixel on one sky axis (exact inverse). See holographic_skydata.world_to_pix.

## smoke

- **`smoke_preset`** -- Run one of the six named SMOKE PRESETS on the wired FFT smoke solver and return its fields
- **`smoke_preset_names`** -- The available smoke preset names.
- **`smoke_step`** -- One smoke step: temperature drives velocity by buoyancy (hot rises), vorticity confinement keeps it
- **`smoke_step_3d`** -- One 3-D smoke step: buoyancy lifts hot fluid along +y, 3-D vorticity confinement keeps it curly,

## smooth

- **`smooth_max`** -- The smooth-maximum partner of smooth_min (iq): smooth_max(f,g,k) is a crease-free INTERSECTION of two
- **`smooth_min`** -- The polynomial smooth-minimum (iq's smin): a soft min(a,b) that MELTS two distance fields together
- **`smooth_sharp_reconstruct`** -- Reconstruct a signal from a two-layer code (CACHE-2): the smooth layer everywhere plus the exact sharp
- **`smooth_sharp_split`** -- Split a signal into a SMOOTH layer (its k_smooth lowest-frequency coefficients) and a SHARP layer (the

## snap

- **`snap_to_grid`** -- GEOMETRIC grid snap (caching_and_storage/holographic_snap) -- snap a 3-D point to the nearest grid node
- **`snap_to_intersections`** -- OBJECT SNAP (K10): snap a point to the nearest INTERSECTION of 2-D polylines ({position, distance}),
- **`snap_to_midpoints`** -- OBJECT SNAP (K10): snap a point to the nearest EDGE MIDPOINT ({edge, position, distance}). See
- **`snap_to_vertices`** -- Snap a point to the NEAREST vertex (holographic_snap) -- returns {index, position, distance} or None if
- **`snap_transform_delta`** -- Snap a TRANSFORM DELTA so the dragged point lands on a target (holographic_snap) -- target

## soft

- **`soft_body`** -- A PBD/XPBD softbody: particles + distance constraints, time-stepped under gravity/forces. Inverse
- **`soft_box`** -- A soft 3-D solid (a lattice of tetrahedra with volume constraints) that resists being squashed and
- **`soft_chain_bank`** -- A TUNING BANK (X8): M variants of one soft-constraint chain differing in stiffness/damping (`hertz` and
- **`soft_chain_matrices`** -- Build an anchored soft-constraint chain's implicit-Euler substep as the affine map `s <- A s + b`
- **`soft_min`** -- S3/C4 -- the log-sum-exp soft minimum, -k*log(exp(-a/k)+exp(-b/k)); k->0 gives min(a,b). This is
- **`soft_relaxation`** -- The per-substep relaxation factor of a SOFT constraint of stiffness `hertz` (natural frequency) and
- **`soft_selection_weights`** -- SOFT SELECTION as a reusable per-vertex WEIGHT FIELD (holographic_meshselect) -- 1 on the selection,
- **`soft_shadow`** -- SDF soft shadow: march each point toward the light; the closest approach to any surface is the

## solve

- **`solve_grid_maze`** -- Solve an N-D grid maze with the Tero slime-mould flow solver -- the SAME solver the 2D maze used, unchanged,
- **`solve_ik`** -- Inverse kinematics by FABRIK (holographic_meshik, FWD-10): move a chain of `joints` (n+1, 3) so the
- **`solve_ik_limited`** -- CONSTRAINED inverse kinematics (holographic_iklimit): reach `target` while keeping each joint within an
- **`solve_laplace`** -- A2 -- solve Laplace/Poisson on an SDF domain with NO MESH and no global linear system: Walk on Spheres
- **`solve_maze`** -- Solve a GridWorld maze by the DETERMINISTIC Tero flow-conductance model (holographic_flow):
- **`solve_pde`** -- WALK ON SPHERES: solve Laplace (Delta u = 0) or Poisson (-Delta u = source) on the interior of an SDF,
- **`solve_poisson_periodic`** -- L5/M2 -- solve laplacian(u) = f on a PERIODIC grid in CLOSED FORM (one FFT; u_hat = f_hat / -|k|^2).
- **`solve_pose`** -- Inverse kinematics for a blendshape rig of STRUCTURES (holographic_blendpose, ARCH-6): solve the blend
- **`solve_waves`** -- Physics backlog (#5): EXECUTE a wave plan -- run each tile's chosen method on the shared surface field

## spectral

- **`spectral_bandwidth`** -- The BANDWIDTH a signal occupies (holographic_bandwidth) -- the fraction of Nyquist (in [0,1]) holding
- **`spectral_basis`** -- The data-driven decomposition basis for a signal on a manifold (holographic_spectral, EXP-6): the
- **`spectral_decode`** -- Invert `spectral_encode`: re-attach the magnitudes to the unit phasors and inverse-DFT back to the
- **`spectral_detail`** -- The fraction of an image's spectral energy above `cutoff` x Nyquist -- "is the sharpness actually STORED?"
- **`spectral_diffusion`** -- A diffusion/heat/gas field as a SpectralField: rate(|k|) = -D|k|^2, closed-form any t. Beats the grid
- **`spectral_encode`** -- Encode an audio frame as an FHRR phasor hypervector -- Puckette's phase-vocoder representation in
- **`spectral_field`** -- Synthesise a SEAMLESS FRACTAL volume (2-D or 3-D) in the Fourier domain -- a 1/f^beta procedural
- **`spectral_flatness`** -- SPECTRAL FLATNESS of a vector (holographic_flatness): the Wiener entropy of its power spectrum (geometric
- **`spectral_landmarks`** -- Farthest-point-sampled landmark indices: a coverage set (every local manifold gets an anchor) for the
- **`spectral_ocean`** -- A deep-water ocean surface as a SpectralField: the dispersive omega(|k|) = sqrt(g|k|) (long swells
- **`spectral_pde`** -- Physics backbone (Part 3 #1): a linear field advanced in FOURIER space by a per-frequency transfer --
- **`spectral_wave`** -- A wave/acoustic/EM(vacuum) field as a SpectralField: omega(|k|) = c|k|, a pulse propagates at speed c;

## splat

- **`splat_aniso`** -- Represent an n-D field (a 2-D image or a 3-D volume) as a superposition of ANISOTROPIC Gaussian
- **`splat_archive`** -- Open a SPLAT-BUNDLE image archive (holographic_splat_archive) -- a gallery stored as Gaussian-
- **`splat_clone_split`** -- SCALE-AWARE CLONE-VS-SPLIT DENSIFICATION (holographic_splatdensify) -- the 3DGS densification DISTINCTION
- **`splat_densify`** -- Fit an n-D field with K ANISOTROPIC Gaussian splats COARSE-TO-FINE (C1) -- 3D-Gaussian-Splatting
- **`splat_field`** -- Represent a 2-D field/image as a SUPERPOSITION of K Gaussian primitives (holographic_splat) --
- **`splat_lod_chain`** -- Build a splat LEVEL-OF-DETAIL chain (holographic_splatprune): prune to each count in `keeps`, measuring
- **`splat_merge`** -- MERGE splats closer than `radius` into one (amplitude-weighted centre and scale, summed amplitude) and
- **`splat_points`** -- IMAGE GENERATION from geometry: render an (N,3) point cloud to an (image (H,W,3), alpha (H,W)) by
- **`splat_prune`** -- PRUNE a splat set to its `keep` highest-contribution splats (largest |amplitude|, since each splat's
- **`splat_region`** -- Read a region's occupancy in [0, 1] back from a splat scene built by splat_scene -- the
- **`splat_relocate`** -- MCMC BIRTH-DEATH RELOCATION (holographic_relocate) -- the successor to evict-rarest. The engine's bounded
- **`splat_scene`** -- Build a CONTENT-ADDRESSABLE splat scene from a field -- a coarse occupancy map stored as ONE
- **`splat_select_lod`** -- Choose a splat LOD by QUALITY budget (holographic_splatprune): the index of the FEWEST-splat level whose

## stokes

- **`stokes_circular`** -- Circularly polarised light: `handedness` +1=right / -1=left, degree-of-circular-polarization
- **`stokes_complex_linear`** -- The complex linear polarization P = Q + iU as a phasor. Sampled over wavelength^2, its FFT is
- **`stokes_faraday_depth`** -- CONVERGENCE door: from a Stokes field + its wavelength^2 axis straight to the Faraday-depth spectrum,
- **`stokes_linear`** -- Linearly polarised light: e-vector at `angle` RADIANS, degree-of-linear-polarization `dop` in
- **`stokes_report`** -- Read a Stokes vector/field OUT in one call: {intensity, dop, dolp, docp, evector_angle,
- **`stokes_to_radiance`** -- Collapse a Stokes field to plain intensity S0 -- the byte-identical 'polarization off' path. See
- **`stokes_unpolarized`** -- Unpolarised light of `intensity` as a Stokes vector [I,0,0,0] -- also what a scalar radiance IS

## stream

- **`stream_decode`** -- Reconstruct the field from one level. Every level decodes to the FULL shape -- a coarse level is a
- **`stream_encode`** -- Encode a field as a PROGRESSIVE payload: {descriptor, levels}. Every byte prefix is itself a valid,
- **`stream_prefix`** -- The richest level that fits in `max_bytes`, or None. Decided from the descriptor alone.
- **`stream_recognize`** -- Sequential recognition over a STREAM of cues bearing on the SAME thing (repeated noisy
- **`stream_report`** -- The ladder, with both norms: {levels, monotone_rms, monotone_max, dense_bytes, best_ratio_at_10pct}.

## suggest

- **`suggest`** -- AGENT-FRIENDLY autocomplete: turn a plain-English task into the best capabilities to use, each with a
- **`suggest_margin`** -- The SMALLEST margin whose replayed hit rate on this query stream meets the target. Empirical by design:
- **`suggest_margin_for_error`** -- THE GATE for any fat-margin cache: the LARGEST margin whose replayed error stays inside the budget.
- **`suggest_pipeline`** -- Propose a PIPELINE from `start_kind` to `goal_kind` (io kinds, see holographic_iokinds) by chaining

## surface

- **`surface_curvature`** -- SURFACE ANALYSIS (K9): Gaussian, mean, and principal curvatures at (u,v) on a parametric surface
- **`surface_intersect`** -- SURFACE-SURFACE intersection (K2, the keystone): trace the intersection curve(s) of two implicit
- **`surface_material`** -- The FIRST-CLASS render material: every channel (color, roughness, reflect, emission, opacity) is a Param
- **`surface_mesh`** -- THE SCULPT LOOP'S RE-EXTRACT STEP (FS-4): turn ANY field representation into the drawable mesh, at the right
- **`surface_mesh_stable`** -- Project a field to a mesh with STABLE vertex identity and a topology guarantee -- the entry point for
- **`surface_nets`** -- DUAL isosurface extraction: (vertices, quads) from a scalar field. One vertex per sign-changing cell at

## synthesize

- **`synthesize_procedure`** -- Construct a SHORT procedure that maps input_vec -> output_vec by bounded breadth-first search
- **`synthesize_program`** -- Fill a VOID CAPABILITY GAP (SYNTH-1): when no registered tool chain reaches a goal, SYNTHESISE one in
- **`synthesize_texture`** -- Inverse-rendering ST2: grow a larger texture from a small sample by Image Quilting -- lay overlapping

## texture

- **`texture_leaf`** -- Make a LEAF for a texture graph. Give `source=` a Texture name ('fbm','voronoi','synth',...) to wrap that
- **`texture_map`** -- Sweep 3 local completion: an image-based TEXTURE MAP sampled by UV with bilinear interpolation -- the
- **`texture_op`** -- Compose a texture-map NODE: an op ('mix','multiply','over','scale','add','remap','min','max','clamp',
- **`texture_projection_error`** -- The ceiling, in a renderer. Interpolating (u,v) linearly in SCREEN space assumes the triangle-to-texture

## validate

- **`validate_kernel`** -- Compile the emitted C with `cc`, RUN it on `calls`, and compare to the Python original: {dialect, n,
- **`validate_materials`** -- Plausibility-check the physical material database (units, ranges, category/phase). Empty list = clean --
- **`validate_plan`** -- Check a plan against ordering rules -- the PB&J test: does every
- **`validate_recipe`** -- Check a StructureRecipe is WELL-FORMED (holographic_recipeops, ARCH-1) -- the recipe's is_manifold():
- **`validate_topology`** -- The full well-formedness report for a mesh (manifold edges AND vertices/bowties, watertight,

## verify

- **`verify_image_structure`** -- Does an image carry the spatial-autocorrelation signature of real data
- **`verify_store`** -- Commit to a list of item vectors with a tamper-evident composition tree (holographic_verify) --
- **`verify_structure`** -- Proof of meaning: does this sequence carry structure, or is it salad?

## workflow

- **`workflow_graph`** -- The WORKFLOW adjacency: the sparse 'bones' of which modules actually work together, derived from the
- **`workflow_neighbors`** -- Which modules WORK WITH `module`, by author-stated cross-reference, best-first as [(module, weight)].
- **`workflow_propagate`** -- Spread per-module scores ONE hop along the workflow bones: a module whose COLLABORATORS are strongly

## zig

- **`zig_batch_eval`** -- Compile a scalar kernel to a native shared library (content-hash cached, `ziglang` wheel, OPT-IN like
- **`zig_dispatch_policy`** -- Z5: which backend (numpy | zig) the native-kernel dispatcher would choose for arrays of length `n`
- **`zig_march_compare`** -- Z4's executed bar: sphere-trace the SAME rays through the engine's Python marcher and a natively
- **`zig_regime_map`** -- Z3's honest measurement: race numpy / zig scalar f64 / zig simd f32 across sizes. Every row carries the

## everything else (alphabetical)

- **`absorb`** -- SELF-ASSEMBLY: hand the mind a pile of `(input, label)` or
- **`abstract_program`** -- Abstract a reusable PROGRAM from a TRACE -- a set of (input_vec, output_vec) examples demonstrating one
- **`accelerator_report`** -- Every optional dependency in one report: installed?, version, what it UNLOCKS (with the measured
- **`acoustic_impedance`** -- Characteristic acoustic impedance Z = rho * c (rayl) of a material, reused from its density x speed of
- **`acoustic_interface`** -- Sound crossing from material A into B: (R, T) = fractions of energy reflected and transmitted, from the
- **`actions`** -- Declare the creature brain's action set. `robust_returns=True` (D2, opt-in) winsorises outlier rewards
- **`admit`** -- Redeem an invite (an Invite or its code) to admit a guest: a scoped Principal of the invite's kind with
- **`advect_field`** -- Transport a scalar field (density/temperature) along a velocity field (semi-Lagrangian).
- **`affected_tests`** -- Which test files actually need to run for a change -- the fix for "why do thousands of tests run on
- **`agent`** -- An upgraded creature agent (AGENT-1): an action LIBRARY as VSA atoms, reward AND pain affect, a
- **`agent_bridge`** -- Connect an OPTIONAL agent to this mind's bus. `llm` is any callable text->reply (your wrapper around any
- **`aharonov_bohm_phase`** -- MEASURE the relative phase the two arms of a ring accumulate from enclosed magnetic `flux` -- the
- **`ambient_occlusion`** -- SDF ambient occlusion at `points` with `normals`: march the normal and read the field -- a near
- **`amplitude_adjusted_surrogate`** -- AAFT surrogate -- the STRICTER null for NON-GAUSSIAN signals (holographic_surrogate). Basic
- **`analog_forecaster`** -- Forecasting backlog (F4): analog forecasting -- "find the past that looks like now, return what
- **`analytic_signal`** -- Decompose a signed series into its ROTATION (holographic_analytic): the
- **`analyze_axes`** -- Which axis of a tensor is the INDEX (carrier) vs the PAYLOAD (content)?
- **`analyze_dataset`** -- Set up the automatic-analysis pipeline over a 1-D signal: register the analyze/decompose/train/
- **`answer`** -- A QUESTION ROUTER -- the honest middle ground between 'completes your
- **`answer_text`** -- Answer a question as a short, CONSTRUCTED sentence -- the surface-realization layer the
- **`anticipate`** -- What does the mind expect next, and how confident is it? Returns
- **`anticipate_meaning`** -- Compose and settle a next-MEANING prediction; return (word, confidence).
- **`ask`** -- A CHAIN over the mind's own memory: ask('paris', ('capital',
- **`ask_traced`** -- ask() instrumented like a PATH TRACER: a relation chain is a ray
- **`assemble`** -- Assemble `target` from a `library` of overlapping fragments by MIN-ENERGY flow search
- **`assemble_pipeline`** -- Find which candidate transform(s) connect input `x` to output `y`, VALIDATED against a shuffle null
- **`assert_robust`** -- Pass only if the LOWER CI bound of a `measure` result clears `floor` -- not just the mean. This is what
- **`asset_library`** -- A fresh AssetLibrary for tracking the EXTERNAL files a scene depends on (textures, models, ...) and repairing
- **`attach_llm`** -- Attach an LLM -- ANY callable text->text (leCore imports no model SDK). Returns an AgentBridge wired to the
- **`attractor_force`** -- A force pulling particles toward a point (negative strength repels). See holographic_fields.
- **`audio_param_bus`** -- Build an audio -> parameter BUS: per-frame band-energy envelopes (bass/low-mid/high-mid/treble by
- **`audio_spectrum`** -- The `k` dominant frequencies (Hz) and their amplitudes in a signal -- the tones that drive a plate or
- **`audit_procedure`** -- Audit a PROTOCOL for honesty anti-patterns (backlog D1): treat an analysis procedure as
- **`axial_similarity`** -- Cosine similarity of two axial values (radians): ~+1 when they are the same orientation,
- **`backend_status`** -- A human-readable line describing the current compute backend (GPU enabled/available/unavailable).
- **`bank_or_formula`** -- Decide whether to BANK computed values or keep the FORMULA and regenerate on demand (Quilez Q1, 'store
- **`best_period`** -- Find the PERIOD of an unevenly-sampled series (Lomb-Scargle): returns {period, frequency, power, fap}.
- **`binding_stability`** -- BINDING-STABILITY regime diagnostic for a key (holographic_flatness): {'flatness', 'distortion',
- **`blackbody_color`** -- The sRGB colour a blackbody GLOWS at temperature `temp_K` -- red ember (~900 K), orange flame, white
- **`blue_noise_sample`** -- Poisson-disk (blue-noise) point sampling by Bridson dart-throwing: a maximal point set with every
- **`bm25_rank`** -- LEXICAL ranking: rank `docs` (list of text strings) by Okapi BM25 against `query` -- exact term
- **`body_params`** -- The neutral character-editor parameter block for humanoid() (holographic_humanoid.default_body) -- every
- **`boiling_point`** -- The boiling temperature (K) at a given pressure, from Clausius-Clapeyron (lower pressure -> boils
- **`break_wave`** -- Physics backlog (#8): set up and run a PLUNGING BREAKER -- a crest whose tip outruns the wave, throwing
- **`brick_ray_index`** -- Build a REGION-keyed ray index: which spatial bricks each ray traversed (eye->hit). Where ray_path_index
- **`browse_capabilities`** -- Browse the capability namespace like a CONTEXT MENU. `by='location'` (default) walks the physical family
- **`bump`** -- G3 -- bump mapping: perturb a mesh's shading normals from a scalar field's slope, NO vertices move.
- **`buoyancy_force`** -- Boussinesq buoyancy force from a temperature (and optional density) field -- hot rises, heavy sinks.
- **`burn_object`** -- An object BEING CONSUMED by fire: `.light()` ignites it, `.step(dt)` advances the burn -- it loses mass
- **`bus`** -- The mind's MessageBus -- one shared bus for this mind that the app, the person, and an agent all talk
- **`cache_interp`** -- Read a gradient cache at query `q` by first-order interpolation with a VALIDITY-RADIUS locality guard
- **`cache_policy`** -- The cache-key consequence of the verdict: {verdict, key_includes_delta, read_set, note}. `invariant`
- **`calibrate_forecast`** -- Forecasting backlog (F1): wrap ANY producer's forecasts in a CALIBRATED prediction interval that
- **`calibration_report`** -- Validate that the false-alarm probabilities recognize() and recall_calibrated() report are
- **`camera`** -- A pinhole Camera (eye looks at target, vertical fov) -- view+projection matrices and per-pixel rays.
- **`camera_controller`** -- Modeling-app feature layer: a viewport camera controller -- orbit/pan/dolly/zoom/frame around a target.
- **`cancel_token`** -- Modeling-app backlog (item F): a cooperative CancelToken to pass as should_stop= to a long render/sim,
- **`canon_storage_report`** -- {classes, raw_floats, total_floats, ratio, zlib_ratio, beats_zlib}. The honest baseline is zlib on the raw
- **`canonical_form`** -- Split an element into (canonical, delta) with `V = canonical @ A.T + b`, EXACTLY (1e-12). `family` is
- **`canonical_shape`** -- The structural fingerprint of a function -- node types and nesting depth, identifiers and constants
- **`canonicalize_procedure`** -- Reduce a BIND/PERMUTE procedure to its MINIMAL equivalent form. The invertible algebra collapses:
- **`capabilities`** -- Query Interface (Phase 6): introspect this mind into a capability REGISTRY -- a VSA table with one row
- **`capability_collisions`** -- Every bare function name that resolves to more than one capability URI -- the semantic collisions, each
- **`capability_confidence`** -- {top, score, margin, confident} -- how dominant the best capability match is. `margin` is the top score
- **`capacity_report`** -- Where this store sits relative to the noise-wins CLIFF (Plate's HRR capacity theory), AND whether
- **`capture_edit_command`** -- Wrap an ARBITRARY geometry edit into a reversible command (holographic_edithistory) by snapshotting the
- **`caustic_focus`** -- Where a reflected pencil is tightest -- the focus / caustic point -- and the pencil radius there. A 5-ray
- **`caustics`** -- Caustics by forward light tracing: shoot parallel light rays, refract them through the object, and
- **`central_mass_from_orbit`** -- Weigh a central body from a bound orbit (holographic_sysid): Kepler's
- **`chain_structure`** -- Build an n-node linked-list CHAIN as a typed structure (B7), at this mind's dim/seed:
- **`chamfer_union`** -- EDGE CHAMFER (K5): the union of f,g with a flat 45-degree chamfer of size r at the crease (the straight-
- **`char_color`** -- The surface colour at burn fraction 0..1: pristine base -> char (blackened) -> ash (grey). The burning
- **`chart_space`** -- Chart a holographic ALPHABET as a measured atlas -- march rays between atoms and record where they
- **`chladni_plate`** -- A vibrating plate whose CYMATIC figures are its Laplacian eigenmodes: `.drive(freqs, amps)` (or
- **`circular_orbit_velocity`** -- The speed for a circular orbit at `radius` around `central_mass`: sqrt(G*M/r). Seeds a stable orbit. See
- **`clifford`** -- Cl(3,0) geometric algebra as a PARALLEL binding mode (holographic_clifford) -- the geometric-product
- **`climb`** -- Walk a relation chain (default is_a) up through the absorbed
- **`climb_ladder`** -- Climb a CORPUS into a TOWER of abstraction levels (the abstraction ladder): consolidate -> find
- **`cloth`** -- A rectangular cloth softbody (structural + shear distance constraints, top row pinned).
- **`cloth3d`** -- A 3-D cloth that drapes under gravity; pass `bending` (a compliance) to add bend springs that
- **`coherent_reflection`** -- Reconstruct the reflection over reflective pixels from a SPARSE trace + gated bilinear interpolation of the
- **`cold_store`** -- A keyed store that bounds memory: keeps at most `keep_warm` values live and compresses the rest, warming any
- **`collide_sdf`** -- ENVIRONMENT collision: push every point inside `sdf_eval` (signed distance < radius) out to the surface --
- **`color_transfer`** -- Inverse-rendering ST1: grade an image toward a REFERENCE image's colour statistics (Reinhard 2001) --
- **`color_waves`** -- Partition conflicting tasks into WAVES that touch disjoint resources: each wave runs fully parallel with
- **`combine_estimators`** -- Veach's balance/power heuristic (MIS): combine several estimators of the SAME quantity, weighting
- **`command_tool`** -- Wrap an allowlisted command as an orchestrator Tool, so the Planner can select and chain it and the
- **`commutator_table`** -- Every claim of the transform tower, as a number. `Aff(n) = GL(n) |x| R^n`: translations are the abelian
- **`comparability_cost`** -- MEASURE the price of binding a boring axis into content (holographic_axisrole):
- **`compiled_sdf_normal`** -- Compile a symbolic SDF's exact normal ONCE and reuse it via the content-addressed compile cache: the
- **`compiled_sdf_numba`** -- SymPy -> Numba, cached: compile a symbolic 3-D SDF to njit scalar+grid value/normal kernels ONCE and
- **`conditional_propagator`** -- Sweep 3 item 9: a CONDITIONAL Propagator -- one learned dynamics operator per ACTION, so predict is a
- **`conflict_graph`** -- Build the CONFLICT GRAPH of a batch of tasks -- `item_keys[i]` is the set of resources task i touches,
- **`conformance_report`** -- Run the ISA conformance suite (ISA-2): check every production base instruction against its
- **`consolidate_subspace`** -- The consolidated low-rank SUBSPACE of stored memories (top-k principal directions) + mean. With
- **`cool`** -- Wrap ONE value so it can be folded up (compressed) when idle and inflated on demand: c = mind.cool(big_table);
- **`cosamp_recall`** -- CoSaMP RECALL (holographic_cosamp, SPEED-3) -- recover the K active atoms of `cue` by BATCH selection with
- **`cosine_palette`** -- iq's cosine gradient palette: turn a scalar `t` (a distance, an iteration count, an orbit trap) into
- **`cosserat_strand`** -- A hair as a COSSERAT ROD (H2b): each segment carries an orientation frame, so the strand HOLDS its
- **`cost_to_go_field`** -- SOLVE THE WHOLE VALUE FIELD ONCE, then route from ANYWHERE for free. One Dijkstra sweep from the goal yields
- **`creature`** -- Build a Spore-style non-humanoid CREATURE from a body-plan spec (holographic_creature) -- a spine with limbs
- **`creature_pose`** -- Build a creature from `spec` and pose its limbs to `targets` ({chain_name: (x,y,z)}) via CONSTRAINED IK in
- **`cross_channel_links`** -- Find DELAYED-COPY / shared-component links between channels
- **`cross_field`** -- The smoothest 4-RoSy field on a CLOSED, ORIENTED surface: per-face angles, as the eigenvector of the
- **`crystal_material`** -- A POLYCRYSTALLINE colour socket f(points)->(M,3): a Worley/Voronoi partition where each grain is a
- **`curl_noise`** -- CURL NOISE: divergence-free procedural turbulence (u, v) on a grid -- the curl of an fBm streamfunction,
- **`database`** -- Query Interface (Phases 9-13): a DATABASE you OWN -- user namespaces over a read-only 'system'
- **`db_query`** -- Run a SQL statement over a Database: CREATE DATABASE, CREATE TABLE ns.t (cols), INSERT INTO ns.t (cols)
- **`decompress_lossless`** -- Recover the exact original sequence from a code produced by
- **`decompress_tensor`** -- Rebuild a tensor from mind.compress_tensor().
- **`dedup_chunks`** -- Content-addressed deduplication of chunk vectors (chunking-transfer item C1): a route that REVISITS
- **`define`** -- The nearest words by learned meaning -- 'what is this word like?',
- **`deform`** -- Apply a classic vectorised deformer to ANY point set -- a Mesh (returns a deformed Mesh, faces kept)
- **`deform_mesh`** -- DEFORM an imported rig at time t: morph-blend the base shape (if it has blend shapes) then apply
- **`deliberate`** -- Think before answering: draft a response, judge it, and refine -- keeping
- **`demux_series`** -- ONE stream, MANY sources (holographic_demux): separate the channels,
- **`density_estimate`** -- Kernel DENSITY ESTIMATE via the encoder (holographic_kde): bundle the encoded samples, then density(x) ~
- **`descend`** -- Walk a plan vector to the branch matching the current SITUATION (a branch-name str, or a state
- **`describe`** -- A human-readable one-line summary of what this mind currently HOLDS: how many memory prototypes over how
- **`describe_skill`** -- A machine-readable SKILL CARD for a capability or a UnifiedMind method by name: what it does + how to CALL
- **`design_network`** -- Multi-terminal network design by the Tero/Physarum flow model -- the 'Tokyo rail' experiment, the
- **`diagnose_bake`** -- For an n-D texture bake of THIS field, should you raise the DIMENSION or the BANDWIDTH (margin)? --
- **`diagnose_scaling`** -- Detect WHICH limit a workload is hitting (holographic_scalinglaw): scale
- **`dictionary_size`** -- How many words the vendored dictionary holds (and its source/license via .manifest()).
- **`dielectric_breakdown`** -- Physics backlog (#7): the raw diffusion-limited branching engine (Niemeyer-Pietronero-Wiesmann). Seed
- **`diffusion_operator`** -- The heat equation's exact PERIODIC propagator as a composable shader-algebra Pipeline: `exp(-alpha|k|^2 t)`
- **`diffusion_transfer`** -- The heat propagator's Fourier transfer, `exp(-alpha|k|^2 t)`, as a plain array. Every mode decays; the DC
- **`directional_field`** -- Sweep 3 item 8: project a DIRECTIONAL function (sampled `values` at unit directions `dirs`) onto
- **`dirty_field`** -- A navigation / physics cost field with DIRTY-FLAG deltas: add movable colliders, then `move` one and only
- **`disc_mask`** -- A circular solid-obstacle mask (1 inside the disc) for fluid_step/smoke_step `solid`. See
- **`dispatch_methods`** -- COMPOSABILITY OF CALCULATION METHODS -- apply a DIFFERENT operator to different elements of one structure,
- **`dispersion_spread`** -- The chromatic angular fan from refracting one ray at several wavelength IORs (eta = n_in/n_out per colour) --
- **`displace`** -- G3 -- displace a surface along its normal by amount*scalar_fn. Dispatches on the target type.
- **`distance_transform`** -- Distance from every cell to the nearest True seed cell, via fast sweeping (same optional-Numba path).
- **`distributed_bus`** -- A DistributedBus: the same publish/subscribe/send bus, but publishes also fan out to peer nodes ('host:port'
- **`distributed_forward`** -- A federated (and optionally deep, cleanup-gated) forward pass in the holographic space -- Path D's
- **`doppler_shift`** -- Forward model: observed wavelength when a source at `lambda_rest` recedes at `velocity` (m/s). The exact
- **`doppler_velocity`** -- Line-of-sight velocity (m/s, positive = receding) from a spectral shift: classical v=c*z, or set
- **`draft_angle`** -- SURFACE ANALYSIS (K9): the moldability draft angle (deg) at (u,v) for a mold pull direction -- positive
- **`draft_vs_refine_simulation`** -- MEASURE whether a coarse simulation is a draft of the fine one. Returns {draft_ms, refine_ms, speedup,
- **`drag_force`** -- Drag force on particles from a fluid, F = k*(v_fluid - v_particle) (fluid->cloth coupling). See
- **`drag_force_3d`** -- Drag on nodes from a 3-D fluid: k*(v_fluid - v_node), sampled trilinearly -- so a softbody couples to
- **`dream`** -- DREAM = generative replay over the consolidated subspace: draw noise, project onto the subspace (the
- **`drift_acceleration`** -- Line-of-sight acceleration (m/s^2) from a narrowband frequency drift rate (Hz/s) at frequency `freq`:
- **`drift_scale`** -- The `variation` probe pointed at a QUERY STREAM instead of at data: the mean step between consecutive
- **`drive_process`** -- Walk a NESTED/fractal process under homeostatic drives, choosing at each node whether to DENOISE,
- **`drive_system`** -- A set of homeostatic DRIVES (DRIVE-1): internal needs (clarity, understanding, coverage, energy) that
- **`dynamics_to_mesh`** -- Export ANY dynamics state as a watertight mesh -- the unified surfacing entry point. `source` is one of:
- **`edit_history`** -- The UNDO/REDO log for an interactive edit session (holographic_edithistory) -- an EditHistory you thread
- **`electrostatic_potential`** -- The electrostatic potential of a charge distribution in ONE spectral step: phi_hat = source_hat /
- **`element`** -- A chemical ELEMENT's engine-relevant properties by symbol: name, atomic number, atomic mass, density,
- **`emergent_concepts`** -- Sweep 3 item 4: online, label-free concept growth -- watch a stream, grow concepts with no fixed
- **`emit_from_surface`** -- Spawn particles ON a surface to DRIVE a particle system: samples the zero level-set of `sdf_eval`, returns
- **`emit_kernel`** -- Emit a scalar, straight-line, float kernel into `wgsl` | `c_f64` | `c_f32` | `js` | `zig_f64` | `zig_f32`. The hand-written
- **`enforce_solid`** -- Force the flow to respect a solid obstacle (velocity -> solid velocity inside the mask, then
- **`enforce_solid_3d`** -- The 3-D immersed boundary: force flow to the solid's velocity inside the mask, then re-project so it
- **`equilibrium_net`** -- Equilibrium Propagation -- the LEARNING RULE for the energy-based (Hopfield) memory the engine
- **`equivariance_table`** -- MEASURE which of {invariant, equivariant, recompute} holds for each (operator, affine family) pair.
- **`escalate_mask`** -- WHERE to escalate: the top `frac` of cells by uncertainty (a fixed budget), or everything at or above an
- **`escalation_plan`** -- THE ESCALATION LADDER (X11): pick {sleep | jump | substep} for one island, per frame. Catto's "4
- **`escape_time`** -- The 2D ESCAPE-TIME fractal FIELD (holographic_sdf) -- Mandelbrot (julia_c=None) or Julia (julia_c=(re,im)),
- **`est_dx`** -- The (dy, dx) translation of frame `b` relative to `a`, recovered by ONE unbind: cross-correlation in the
- **`estimate_bill`** -- 'Render' the mass, cost, and embodied carbon of a bill of materials [(material, volume_m3), ...] by
- **`estimate_light_direction`** -- Inverse-rendering IR3: a COARSE sun-direction estimate (azimuth, elevation) from an image's brightest
- **`evaluate_elements`** -- **Part C, end to end.** Hand it RAW point sets with no shape ids: `canonmesh.recognize` derives the
- **`evolving_atom`** -- SUBSTRATE EVOLUTION -- a context-conditioned atom that updates its OWN harmonic coefficients as
- **`execute_plan`** -- RUN a discovered, proven plan -- the loop from discovering structure to
- **`execution_replay`** -- Run a long program CHUNKED and record a verifiable, O(change) REPLAY LOG (DELTA-1 x WIRE-2): each
- **`explore_series`** -- AUTO-EXPLORE an unlabeled multi-axis series (holographic_scaffold): try
- **`export_splats`** -- EXPORT Gaussian splats to a browser-renderer format (holographic_splatexport, FS-3) -- so a field/scene
- **`export_splats_2d`** -- Export 2-D image splats (splat_fit's (cy, cx, amp, sigma)) to the standard 3DGS .ply by lifting them
- **`extend_generator`** -- FORECAST by playing a fitted generator PAST its data (store the formula, play the future). Given a
- **`extract_roles`** -- Parse a request or docstring into a holographic role record {action, object, quality} over the
- **`extract_template`** -- DISCOVER the generic schema and its context-bound slots in a repeated
- **`face_field_to_vertex`** -- Average a per-FACE 4-RoSy field (cross_field angles) to a per-VERTEX tangent-plane direction -- the input
- **`factor_composite`** -- Pull a single bound composite APART into the factors that built it -- the inverse of binding,
- **`factored_field_report`** -- The W1 comparison, carried WITH the capability: run a separable blur dense and factored, and report
- **`faculties`** -- The capability table (Sweep 3 item 1): the sorted names of every faculty currently callable from a VSA
- **`family_verdict`** -- The verdict for a composite canonicalisation family (`rigid` | `similarity` | `affine`): the WEAKEST of
- **`faraday_rm_map`** -- TELESCOPE-AS-OBSERVER: recover a per-pixel Faraday-depth (line-of-sight magnetism) MAP from a sky
- **`faraday_rotate`** -- FORWARD Faraday model -- the polarized sky a telescope receives: rotate an intrinsic Stokes signal
- **`farm`** -- A distributed-compute Coordinator over a NetworkFarm of remote worker nodes ('host:port' each running
- **`fast_creature_encoder`** -- Compiled, fully in-VSA perception for the creature loop: a FastCreatureEncoder whose per-step
- **`fd_gradient`** -- The central finite-difference gradient of a scalar function f: R^n -> R at x (holographic_optimize,
- **`federated_archive`** -- A FEDERATED image archive (holographic_archive.FederatedArchive) -- the storage array's federation
- **`federation_report`** -- A federation / conservation diagnostic -- Path D's 'as above, so below' law as a callable readout,
- **`fetch_field`** -- Query a baked field at any point: one dot product. See holographic_shader.fetch.
- **`fetch_field_nd`** -- H5 -- query an n-D baked field at any point: one dot product (two, normalized).
- **`fft_backend`** -- Report or switch the FFT backend behind bind/bundle. Default 'numpy' is bit-exact and deterministic;
- **`fft_benchmark`** -- Reproduce the numpy-vs-pyFFTW comparison (ratios <1 mean pyFFTW is slower). Documents why numpy stays
- **`fill_capability_gap`** -- The orchestration: if a registered tool/chain already reaches the goal (`registry_hit` >= threshold),
- **`fill_report`** -- Score a fill ON THE HOLES ONLY: {n_holes, hole_fraction, mae, accuracy}. Scoring the known cells would
- **`filter_limit`** -- H6 -- the N -> infinity steady state of a circular filter, in closed form: an idempotent PROJECTION onto
- **`filter_passes`** -- H6 -- apply N passes of a circular filter in ONE evaluation. A GPU runs the kernel N times; a bind is
- **`finding_registry`** -- The findings registry (backlog D3): a research log as a holographic KNOWLEDGE STRUCTURE. Record
- **`fire`** -- A material-aware BURNING body: `.step(dt)` checks ignition against the material's autoignition point,
- **`flat_recall`** -- The BASELINE hierarchical_recall must beat: unbind both roles and clean up ONCE at the bottom, with no
- **`flow_circulation`** -- Decompose a solved Tero/Physarum flow into TRANSPORT and CIRCULATION -- the analysis layer the flow
- **`fold_fit`** -- INFER a fold RECIPE from an observed structure (holographic_foldfit) -- the inverse of fold_fractal.
- **`fold_fractal`** -- The KALEIDOSCOPIC-IFS / MANDELBOX distance-estimator SDF -- the general FOLD ENGINE behind the fractal-
- **`forward_forward`** -- The Forward-Forward algorithm -- backprop-free, settling-free DEPTH from purely LOCAL objectives
- **`four_surface_demo`** -- ONE KERNEL, FOUR SURFACES (W19): given a single SDF scene (node or DSL text), return its FOUR
- **`frechet_mean`** -- The Frechet (Karcher) mean of unit vectors -- the geometrically-correct average on the sphere, the
- **`free_surface`** -- Physics backlog (#8, rung 4): the OVERTURNING free-surface solver -- particles that can fold the water
- **`from_state`** -- Rebuild a UnifiedMind from to_state(). The reloaded mind classifies and decides identically;
- **`function_purity`** -- Is the module-level function `name` in `source` provably PURE? CONSERVATIVE: unknown means False. A
- **`gated_traverse`** -- Drive an iterative holographic traversal with a THROUGHPUT GATE -- Russian roulette for a path
- **`grain_material`** -- A WOOD-GRAIN colour socket f(points)->(M,3): concentric rings along an axis + lengthwise fibre streaks +
- **`grant`** -- Share selectively: grant a principal READ access to a namespace (or list of them). See holographic_access.
- **`greeble`** -- S2 -- cover a base mesh's faces with extruded greeble boxes (the G5 panel idea on any surface) ->
- **`grid_graph`** -- Adjacency dict {cell: [neighbours]} for an N-D grid (a 2D/3D/.../ND maze is the same object). Feed to any
- **`grid_to_hypervector`** -- Encode a NumPy field (a fluid density, an SDF slice) as an FPE hypervector so it can be tiled /
- **`groom_hair`** -- HAIR GROOM (H1): grow `n_strands` rooted on an SDF surface, each along its outward normal (+ optional
- **`ground_plane_depth`** -- GROUND-PLANE DEPTH from linear perspective: for a forward-looking camera (road, railway, hallway) the
- **`group_objects`** -- Modeling-app feature layer: group objects under a null parent (grouping = a bundle); one undo step.
- **`grow_ice`** -- Physics backlog (#7): grow an ICE / frost dendrite by diffusion-limited branching (the dielectric-
- **`grow_lightning`** -- Physics backlog (#7): grow a LIGHTNING bolt -- the SAME diffusion-limited branching engine as the ice
- **`guided_cross_field`** -- A GUIDED 4-RoSy field: the smoothest field that ALSO aligns to a prescribed per-face direction where one
- **`guided_upsample`** -- Inverse-rendering ST3: guided (joint-bilateral) super-resolution -- render colour SMALL, then upscale it
- **`gyroid_field`** -- The GYROID triply-periodic minimal surface as an implicit FIELD (surface at f=0): sin x cos y + ... .
- **`hair_wind`** -- CURL-NOISE WIND (H7): a divergence-free (volume-preserving) turbulent wind field; call `.force(strand)`
- **`haze_depth`** -- RELATIVE DEPTH from a single HAZY/FOGGY image via the atmospheric scattering model (Tarel-Hautiere veil
- **`heat_body`** -- A lumped body of a named `material` at a uniform temperature: `.add_energy(Q)` raises it by Q/(m c),
- **`helix`** -- A HELIX curve: n points spiralling `turns` times at `radius`, rising `pitch` per turn. (n, 3). Sweep
- **`hierarchical_pack`** -- Superpose G group-keyed CHUNKS into one vector, each chunk itself a pack of its leaves. Deliberately just
- **`hierarchical_recall`** -- Descend a hierarchical superposition with a CLEANUP at the middle level: unbind the group key, snap the
- **`high_capacity_memory`** -- An opt-in FHRR (complex-phasor) key->value trace memory and its atom vocab,
- **`hodge_decomposition`** -- Split an edge flow into three L2-orthogonal parts (holographic_spectral, EXP-8): gradient (curl-free
- **`holo_octree`** -- A capacity-adaptive 3D holographic octree (TILE3D-1): tile 3D space so each node carries its points as
- **`hsv_to_rgb`** -- Vectorised HSV->RGB (all args in [0,1], hue wraps) -- the natural map for cyclic quantities like a
- **`human_observer`** -- The human eye as an OBSERVER (CIE 1931 colour-matching functions on 380-780 nm). Feed it a spectrum
- **`humanoid`** -- Build a parametric biped HUMANOID with automatic IK rigging and CHARACTER-EDITOR body morphs
- **`iaaft_surrogate`** -- IAAFT surrogate -- the gold-standard null matching BOTH the exact amplitude distribution AND (to
- **`ideal_gas`** -- A parcel of gas in a definite state: `.density()`, `.sound_speed()`, `.adiabatic_change(V2/V1)`. The
- **`ifs_fit`** -- Match a 2-D `target` point cloud to the CLOSEST NAMED affine-IFS system (holographic_ifs) -- the honest
- **`ifs_generate`** -- Generate a plant/fractal point cloud from an AFFINE IFS via the chaos game (holographic_ifs). Pass a named
- **`ignites`** -- True if `material` is at or above its autoignition temperature (hot enough to catch fire); False below
- **`iht_recall`** -- IHT RECALL (holographic_iht, GRAD-1) -- recover the K active atoms of `cue` (a bundle of `codebook` rows)
- **`import_asset`** -- Import an artist file by extension: .obj (+its .mtl) or .glb/.gltf -> a LoadedMesh (geometry + PBR
- **`inception`** -- One-parameter recursion DEPTH over fractal_volume + an honest capacity-ceiling measurement. Returns
- **`incremental_renderer`** -- A render SESSION: render the first frame, then re-render the SAME scene for FREE (cached), apply colour/
- **`index_procedures`** -- Build a behavioural FINGERPRINT index: run each procedure ONCE on a canonical probe and
- **`index_route`** -- Build a sub-linear RANDOM-ACCESS index over a chunked route (from plan_route / chunk_route): a BVH
- **`ingest_files`** -- Digest a FOLDER, a .zip, or a single file into a queryable FILE MAP: mind.ingest_files('project/') or
- **`inpaint`** -- Fill the unknown cells of a field. `known` is a boolean mask, True where the value is trusted.
- **`instance`** -- Modeling-app feature layer: create an instance sharing a source's geometry with its own transform
- **`instanced_scene`** -- An empty INSTANCED scene: place shared Definitions through transforms (scene.place(defn, transform)) so many
- **`instantiate_template`** -- Instantiate a named parameterized template (ISA-6) at this mind's dim/seed, filling its HOLES with
- **`integrate_normals`** -- Inverse-rendering IR7: integrate a tangent-space normal map into a single-valued, CONSISTENT height
- **`interpolate_hair`** -- GUIDE INTERPOLATION (H3): make many render strands from a few simulated guide strands by blending the
- **`invite`** -- Create an invite token admitting a guest as `kind` with initial READ grants (e.g. {'read': ['lab/scene']}).
- **`io_kinds`** -- The closed vocabulary of io DATATYPE kinds a capability can consume/produce (holographic_iokinds) --
- **`iridescent_tint`** -- MATERIAL: the iridescent RGB tint of a thin film (soap-bubble / oil-slick sheen) of `thickness_nm` seen
- **`irradiance_cache`** -- Global illumination via a sparse IRRADIANCE CACHE (Ward = the engine's adaptive-anchor idea): compute
- **`island_energy`** -- The SLEEP SENSOR for one island: a monotone 'how active is this' scalar (kinetic + displacement from
- **`island_sleep_tracker`** -- A per-island awake/asleep tracker with HYSTERESIS: an island sleeps after `sleep_frames` consecutive
- **`islands`** -- Partition n_nodes into ISLANDS -- the connected components of a constraint graph, under an undirected
- **`kde_bandwidth`** -- The RBF kernel bandwidth for a density estimate over [lo,hi] (holographic_kde): 'lcv' (leave-one-out
- **`kepler_ellipse`** -- A whole orbit as n (x,y) points with the star at a focus (perihelion a(1-e), aphelion a(1+e)); uniform in
- **`kepler_position`** -- Position (x,y) on a Kepler orbit at a given phase (mean anomaly, radians), star at a focus. Field-native
- **`kernel_from_description`** -- Generate a geometry KERNEL from a controlled-vocabulary description (C3): registered parametric SDF
- **`kinematics`** -- CLOSED-FORM KINEMATICS on the substrate (holographic_physics, Kinematics) -- physics as an algebra of
- **`klein_bottle`** -- A KLEIN BOTTLE (figure-8 immersion) as a point grid -- the classic non-orientable surface showpiece.
- **`lattice_deform`** -- Free-form (FFD) deformation through a control lattice: each point moves by the TRILINEAR interpolation
- **`layered_material`** -- Stack material LAYERS bottom-to-top with the ORDER enforced: base < diffuse < specular/reflection <
- **`levitation_chamber`** -- ACOUSTIC LEVITATION: beads in a vertical standing wave feel the Gor'kov radiation force and are trapped
- **`light`** -- A Light: 'directional' (sun), 'point', or 'ambient' (fill). See holographic_render.Light.
- **`light_shafts`** -- Volumetric LIGHT SHAFTS / god rays by radial blur (W16, Mitchell GPU Gems 3): streak the bright pixels
- **`lobe_sigma`** -- The Gaussian lobe half-width of the whole secondary bundle at distance s: geometric pencil spread combined
- **`local_structure`** -- Classify a point by the shape of its local neighbourhood (the 'cosmic web' method extracted from leOS):
- **`lomb_scargle`** -- The Lomb-Scargle normalised power at each trial frequency (cycles/time) for unevenly-sampled data --
- **`lomb_scargle_auto`** -- Give me the periodogram of this light curve: builds the frequency grid from the data and returns
- **`look_at`** -- Modeling-app backlog (item G): an OpenGL view matrix for a camera at `eye` looking at `target`. See
- **`lookup`** -- Look a word up in the VENDORED DICTIONARY (~144k English words): its definition, part of speech, synonyms,
- **`lorentz_force`** -- Physics backlog (#6): the Lorentz force F = q(E + v x B) on a charge q moving at v through fields E, B.
- **`low_discrepancy_sample`** -- `n` low-discrepancy (quasi-random) points in [0, 1)^d -- even coverage of a domain. The right
- **`low_rank_field`** -- Factor a 2-D field into a LowRankField and operate on it WITHOUT ever materialising it: .blur(k) (a
- **`lsystem`** -- G5 -- a context-free L-system grammar; productions are a holographic record, output is a scenegraph.
- **`maintain_now`** -- Reorganize the memory and refresh the brain, each by its own held-out
- **`majority_fill`** -- Fill a CATEGORICAL field's holes by neighbour vote, one ring per sweep. Ties break toward the lowest
- **`mandelbulb`** -- The MANDELBULB distance-estimator SDF (holographic_sdf) -- White & Nylander's polar-power fractal, the 3D
- **`map_bind`** -- MAP binding: elementwise product. Commutative AND self-inverse (bind(x, x) is the all-ones vector),
- **`map_codebook`** -- A deterministic codebook of `n_codes` random bipolar (+/-1) vectors of length `dim`. Regenerated from
- **`margin_cache`** -- Cache a baked result over an ENLARGED region around a DRIFTING query (a camera, a cursor, an agent, a
- **`market_projector`** -- Forecasting backlog (F7, de-silo): the RayProjector time-series study -- casts rays into a data field
- **`mass_to_temperature`** -- A star's main-sequence temperature (K) from its mass (solar units): T ~ 5772*M^0.525 (1 Msun -> Sun). A
- **`match_prototype`** -- UNSTRUCTURED classification (the twin of match_record): when an item has NO role schema -- it is a
- **`match_record`** -- DOMAIN-GENERAL structured matching: rank `candidates` ({name: {role: filler}}) by how well their
- **`matter_step`** -- Advance a Mixture one step on ONE shared incompressible flow: blend density -> buoyancy -> a single
- **`maxwell_field`** -- Physics backlog (#6): a 1-D coupled Maxwell field (Yee/FDTD) -- Ez and Hy feed each other so a pulse
- **`meaning_prediction_report`** -- Exact-symbol accuracy and semantic RANK (did the composed prediction
- **`mellin_promotes_scale`** -- **A layer you cannot diagonalise, you relocate.** On a LOG axis a dilation becomes a translation, joins
- **`mellin_scale`** -- Recover the DILATION alone, via the Fourier-Mellin lift. KEPT NEGATIVE: the SUPPORT BAND is the gate,
- **`memoize_pure`** -- Memoize `fn` on (its exact canonical source, its arguments) -- and REFUSE if `fn` is not pure.
- **`menger_fractal`** -- S1 -- the canonical Menger-sponge FRACTAL model as an SDF (a box minus recursive crosses). Evals,
- **`merge_forks`** -- Reconcile several forked worlds, each a {slot: vector} delta layer. Slots the forks AGREE on merge
- **`metaball_mesh`** -- METABALL MESH (soft-blob base mesh): sum-of-Gaussians field at `centers` (n,3), spread `radius`, marched
- **`milk_eval`** -- Evaluate ONE ns-eel2 expression string (Milkdrop's equation language) against a variable dict
- **`milk_parse`** -- PARSE a Milkdrop `.milk` preset's TEXT into a MilkPreset (holographic_milkdrop) -- settings +
- **`mirror_mesh`** -- Mirror a mesh across the `axis`=const `plane`: append a reflected copy with reversed winding (normals
- **`mis_recover`** -- Recover a vector by combining HARD 1-NN and SOFT (dense-Hopfield) cleanups per-query via the balance
- **`mixture_of_experts`** -- MIXTURE OF EXPERTS with a LEARNED GATE (holographic_moe, GatedMixture) -- a bank of specialists plus a
- **`modal_solver`** -- A stateful ModalSolver: set_mode(key, A, b) on every contact-mode switch, advance(k) to jump within a
- **`mode_key`** -- A deterministic signature for a contact mode (the active-constraint set), so the same mode reached by
- **`model_tolerance`** -- The document's single tolerance authority (K11): the abs/rel/angular tolerances a boolean, a snap, and
- **`modifier_stack`** -- Modeling-app backlog (item C): a per-object MODIFIER STACK + dependency graph over any payload (mesh /
- **`monotone_cost`** -- MEASURE the price of clockwise-only (one-way) rotation on a REAL signed
- **`morph_scene`** -- Morph between two images. method='dct' (default) blends in the DCT-coefficient domain (structure
- **`mueller_matrix`** -- Build the 4x4 Mueller matrix of an optical element by `kind`: 'identity', 'polarizer' (at
- **`multi_horizon_forecaster`** -- Forecasting backlog (F6): multi-horizon forecast with a TRUSTED-HORIZON gate -- calibrate a per-step
- **`multi_material`** -- Combine N Materials by per-point MASKS -- generalises Material.blend (2-way, one scalar) to N materials
- **`multires_pyramid`** -- Build an anti-aliased mipmap of `signal` -- [full, half, quarter, ...], each level low-pass filtered
- **`mutual_information`** -- Mutual information I(X;Y) in BITS between two equal-length signals (discrete or continuous, continuous
- **`mutual_information_vs_null`** -- Mutual information ABOVE its SHUFFLE NULL -- the honest dependence measure. Computes raw MI, then a null
- **`navigator_benchmark`** -- The honest comparison, agent-callable: {recall, comparisons, fixed_beams:[{beam, recall, comparisons}]}.
- **`navigator_find`** -- Search the trained navigator's data tree for `cue` (a (D,) vector): {index, comparisons, trace}.
- **`near_surface_to_sdf`** -- Sweep 3 local completion (photo-to-3D): turn a NEAR-SURFACE signed field (accurate only in a thin band
- **`nearest_in`** -- Nearest row of a codebook matrix to `query` -- argmax(matrix @ query) (nearest), the reusable
- **`negotiate`** -- Deliberate under competing judges (coherence vs novelty vs relevance):
- **`nested_scene_structure`** -- compose_nested AS a typed structure: the same super-scene vector, now a replayable build-graph
- **`new_scene`** -- Modeling-app backlog (item 0): a fresh canonical Scene document -- the single source of truth a modeling
- **`next_symbol`** -- Predict the next symbol (character) that should follow `context`, using a learned FLAT n-gram sequence
- **`node_graph`** -- NODE EDITOR backend: a heterogeneous typed node graph an editor binds to -- add nodes, connect sockets
- **`node_registry`** -- NODE EDITOR backend: the default node-type palette wired to real leCore compute (SDF primitives + CSG +
- **`nonnewtonian_fluid`** -- A fluid solver with NON-NEWTONIAN rheology: its viscosity is the shear-rate-dependent power law, so it
- **`novel_object_specs`** -- Every (colour, shape, texture) combination the scene coder can compose -- including the ones never
- **`nystrom_embedding`** -- The SCALABLE spectral embedding (SCALE-1): the smooth eigenbasis of `spectral_basis` without the dense
- **`nystrom_field`** -- Approximate a kernel-weighted field f(p) = sum_j weights[j]*K(p, sources[j]) (Gaussian RBF) via m
- **`observe_sequence`** -- Run the predictive loop over a token sequence; return the Steps (the
- **`observe_spectrum`** -- Integrate a spectrum (sampled on the observer's wavelengths) against each channel -> readings
- **`observer_receptor_bank`** -- Build a bank of Gaussian receptor sensitivities (the generic shape of biological cones) on a
- **`occlusion_recall`** -- OCCLUSION RECALL (holographic_occlusion, RT-V) -- recover the components present in `cue` (a bundle /
- **`occlusion_recall_forest`** -- OCCLUSION RECALL, FOREST-ROUTED (holographic_occlusion, SPEED-2) -- occlusion recall with the per-step
- **`occupancy_to_mesh`** -- Extract a surface MESH (vertices, quads) from an occupancy grid via surface_nets -- closes the round
- **`oct_decode_normals`** -- Decode octahedral integer codes (N,2) back to unit normals (N,3) (holographic_octnormal). Inverse of
- **`oct_encode_normals`** -- OCTAHEDRAL-encode unit normals to compact integer codes (holographic_octnormal, Cigolle et al. 2014):
- **`offset_curve`** -- CURVE OFFSET (K4): a parallel polyline at distance `dist` (positive grows a CCW loop outward), with the
- **`opponent_channels`** -- Decompose the disagreement between TWO estimates of the same thing (the opponent-processing decomposition,
- **`optimize`** -- GENERAL GRADIENT-DESCENT OPTIMIZER (holographic_optimize, GRAD-2) -- minimize any scalar `loss(x)` from
- **`optimize_toolchain`** -- DIFFERENTIABLE ORCHESTRATION -- optimize a whole tool-chain JOINTLY against a chain-level
- **`orbit_trap_render`** -- Render an SDF scene coloured by ORBIT TRAP -- the signature Quilez fractal look, in one call. Sphere-
- **`orient2d`** -- Exact-sign 2D orientation predicate: +1 if c is left of a->b (ccw), -1 right, 0 exactly collinear
- **`orient3d`** -- Exact-sign 3D orientation predicate: +1 if d is above the plane a-b-c, -1 below, 0 exactly coplanar.
- **`oxidation_field`** -- A CORROSION front over a surface grid: rust/patina that NUCLEATES at exposed/wet faces (default: the
- **`oxide_color`** -- The blended colour of a material at oxidation fraction 0..1 -- pristine base to full oxide (rust orange,
- **`pack_benchmark`** -- Should you set-pack this family at all? Returns rows (name, bytes, psnr) for raw, per-file PNG,
- **`pack_images`** -- Pack a FAMILY of 8-bit images as ONE reference plus per-image deltas, entropy-coded with zlib. Lossless
- **`packet_demux`** -- Demultiplex a PACKETIZED stream (holographic_demux): variable-length
- **`pairwise_repulsion`** -- Short-range particle-particle repulsion (the n-body short-range force), CULLED by spatial_hash_pairs
- **`parallel_transport`** -- Transport a tangent vector `v` (a 'displacement', a move from one state to another) from the tangent
- **`param`** -- Make a connectable parameter SOCKET: a value that is a constant OR wired to a map / field / named output --
- **`parse_scene_description`** -- Parse a controlled 3-D scene DESCRIPTION into objects + environment (holographic_semantic). Each object is
- **`particle_sim`** -- Render/Sim Pipeline (Phase 0 / G2): a point-mass sim advanced by the shared, energy-stable symplectic
- **`particle_system`** -- A ParticleSystem on the grid: particles feel forces (gravity, attractors, any (N,2) force a VSA
- **`partition_domain`** -- Decompose a domain of n items into k disjoint buckets for distribution. With `costs` (a per-item work
- **`partition_grid`** -- Decompose a 2D image/field (shape=(H,W)) into TILES or a 3D volume/grid (shape=(X,Y,Z)) into BRICKS -- the
- **`path_cost`** -- Total field cost accumulated along a path -- for comparing a navigated route to a naive straight shot.
- **`path_trace`** -- Monte-Carlo PATH TRACER for true multi-bounce global illumination -- the core of V-Ray/Redshift/Arnold.
- **`pattern_field`** -- A named deterministic procedural pattern FIELD f(points)->[0,1] (checker, stripes, gradient, dots, noise,
- **`pbr_material`** -- A standard glTF 2.0 metallic-roughness PBR material (the ISO factor model: base colour RGBA, metallic,
- **`perceive`** -- Any input -> one vector in the shared space. This is the only encoder in the
- **`period_false_alarm`** -- The chance a peak this strong comes from the sampling window alone, via a permutation null (times fixed).
- **`permutation_null`** -- The shuffled-null test as ONE composable primitive: score your real datum, then re-run the IDENTICAL
- **`phasor_monotone_cost`** -- MEASURE the group-vs-monoid price where it actually lives: a TRUE complex
- **`photo_to_3d`** -- Forecasting sweep (sec.5, depth delegation) / photo-to-3D: lift a depth map + image into per-pixel 3D
- **`physical_material`** -- The PHYSICAL properties of a named material (density kg/m3, viscosity, Young's modulus, refractive index,
- **`physics_compression_report`** -- The event codec's size AND every baseline it claims to beat, computed together so an agent can re-run
- **`pick_element`** -- VIEWPORT PICKING for a 3D-modeling app (holographic_framebudget): given a wireframe cage and a screen
- **`pick_mesh`** -- VIEWPORT PICK on a REAL mesh (holographic_raypick) -- from a cursor (screen_u, screen_v in -1..1), build
- **`pipeline_map`** -- The WHOLE workflow graph as data: every typed edge (consume_kind -> produce_kind -> capability)
- **`pipeline_report`** -- The findings recorded by the last analysis pipeline run (topology, explained variance, n_terms,
- **`pivot_index`** -- A recursive pivot-tree index for SUBLINEAR nearest-item recall (Path D, the forest/data-structure
- **`pivot_point`** -- Resolve the PIVOT for a transform (holographic_transform_space) -- 'median' (centroid), 'bbox' (box
- **`place_sampler`** -- Modeling-app backlog (capstone): drop a Sampler into the Scene as an object (handle + transform), so it
- **`planet_field`** -- Regenerate a planet's actual surface field from its star_system recipe entry, via fractal_planet (the
- **`point_in_brep`** -- B-REP MEMBERSHIP (toward K6 booleans): is each point inside the solid? Delegates to the generalized
- **`points_to_mesh`** -- The whole path: oriented points -> SDF grid -> watertight quad mesh. Returns (verts, quads, field, grids)
- **`polarization_readout`** -- Read polarization from a Stokes field the mantis way: linear detectors + circular detectors via a
- **`polylines_to_dxf`** -- CAD EXPORT (K7): a minimal DXF R12 ASCII string for 2-D polylines (POLYLINE/VERTEX; closed loops flagged).
- **`position_field`** -- IFAM POSITION FIELD (4-PoSy): optimise a per-vertex LATTICE position aligned to the orientation field, by
- **`position_field_regularity`** -- How LATTICE-REGULAR a position field is: mean per-edge residual of (p_i - p_j) after removing the nearest
- **`post_process`** -- Apply a post-processing PROGRAM (a holographic_postfx.PostChain -- an ordered, named chain of effects) to a
- **`power_law_viscosity`** -- The non-Newtonian power-law viscosity eta = K * shear_rate^(n-1): a viscosity that DEPENDS on how fast
- **`precedes`** -- In the stored plan, does step a come before step b? The order
- **`prediction_report`** -- How well does the mind anticipate this sequence (no learning)? Returns
- **`predictive_filter`** -- A SURPRISE filter (holographic_extras) -- observe(vec) returns (is_novel, surprise); slow drift is
- **`preview_material`** -- Render a material on a preview SPHERE -- the classic MATERIAL BALL. Works on a plain Material or a CMP2/CMP3
- **`preview_texture`** -- Render a CMP1 texture graph as a flat RGB SWATCH -- a (res,res,3) float image in [0,1] you can save/view.
- **`principal`** -- A scoped Principal identity for an actor (agent/user/service/peer), wired to this mind's db, bus, and (if
- **`probability_current`** -- The probability current j = (hbar/m) Im(psi* grad psi) - (q/m) A |psi|^2 of a wavefunction, as [jx, jy].
- **`procedural_compression`** -- S3/C1 -- measure procedural representation AS compression: the tiny generator (DSL) vs the
- **`procedural_noise`** -- G1 -- holographic band-limited procedural noise as a FIELD; fBm as an octave BUNDLE.
- **`procedure_to_recipe`** -- Express a procedure as a typed StructureRecipe (the B7 structure object) -- proving a program
- **`project_onto_constraints`** -- Satisfy a set of constraints on a vector by ITERATED PROJECTION -- sweep a list of projections
- **`project_uv_from_shell`** -- PROJECT a UV map from a texture-carrying SHELL onto a new mesh of ANY topology: for each new vertex, find
- **`propagator_jump`** -- Jump a learned dynamics operator k steps in ONE eval (RT-I1): the closed-form k-step iterate via the
- **`propagator_spectrum`** -- Read a learned dynamics operator's convergence off its FREE FFT spectrum (RT-I1) WITHOUT running:
- **`proportional_edit`** -- PROPORTIONAL EDIT (Blender O + G): move the selected vertices by `translate` and drag their neighbours
- **`prototype_classifier`** -- Gradient-free CLASSIFICATION -- the HDC/VSA prototype learner, the other truly derivative-free
- **`prove_executable`** -- SELF-PROOF that a discovered order is VALID, not merely predictable.
- **`purity_report`** -- Purity verdicts for every module-level function in `source`, closed over the CALL GRAPH: a function that
- **`purity_scan`** -- Run the purity analysis over a whole tree, merging every module's functions into ONE call graph so a
- **`push_charge`** -- Physics backlog (#6): integrate a charged particle through uniform fields E, B with the Boris pusher
- **`pyramid_reconstruct`** -- Resample a pyramid level (from multires_pyramid) back to length `n` -- the LOD read, so a coarse,
- **`quad_remesh`** -- FIELD-GUIDED tri-to-quad RETOPOLOGY: pair adjacent triangles into quads, preferring pairs whose quad edges
- **`quadruped_spec`** -- A ready-made creature body plan -- a quadruped (spine + two mirrored leg pairs + head)
- **`quantity`** -- A physical QUANTITY = value + unit + uncertainty + source, in the dimensional GRAMMAR (holographic_
- **`radiance_to_stokes`** -- Lift a scalar/RGB radiance to UNPOLARISED Stokes (S0=value, rest 0). Round-trips byte-identically
- **`radiance_transfer`** -- PRECOMPUTED RADIANCE TRANSFER -- collapse the light-transport integral into a per-point transfer vector once,
- **`random_palette`** -- A random-but-harmonious cosine palette from a seed -- the 'regenerate from seeds' lever for COLOUR
- **`rank_for_error`** -- The SMALLEST rank whose truncated SVD reconstructs `X` to within `max_abs_error` in the MAX-ABS norm.
- **`rate_distortion_report`** -- Duda's question, answered honestly: what is the CHEAPEST bit budget that stores these vectors while
- **`reaction_diffusion`** -- Sweep 3 item 3: a reaction-diffusion cellular automaton (HyperCA) -- a local update rule over a
- **`realtime_session`** -- A reprojecting viewport over a `RenderSession`: `frame()` is a DRAFT that shades only the news,
- **`recolor_image`** -- 2D EDIT -- grade an image toward another image's COLOUR statistics (a colour-transfer / recolour). `mode`
- **`reconstruct_from_anchors`** -- Piecewise-linear reconstruction of field `y` (sampled at `x`) from its values at `anchor_x` (CACHE-3) --
- **`reconstruct_tower`** -- Expand a climbed ladder TOWER back to its ORIGINAL corpus of base symbols -- the inverse of
- **`record_physics_trace`** -- Record the reference bouncing-box trace: `n` bodies under gravity in a restitution box. Returns
- **`recover_affine`** -- Recover the canonical (S, T) of an arbitrary translate/scale edit history: `after(x) = before((x-T)/S)`.
- **`recover_scene`** -- Inverse-rendering IR4 (the headline): analysis-by-synthesis. Given a TARGET image, gradient-free-search
- **`rectify_carrier`** -- REPAIR a nearly-boring carrier axis into a clean uniform index
- **`recurrent_forecaster`** -- Forecasting backlog (F7, de-silo): a gradient-free sequence producer, now reachable through the mind.
- **`recursive_factor`** -- Factor a DEEP composite by solving a SHALLOW problem over composed chunks, then expanding by lookup.
- **`redshift`** -- Redshift z = lambda_obs/lambda_rest - 1 (positive = receding). Field-native. See
- **`reduce_involution`** -- Reduce a leaf multiset modulo MAP's self-inverse binding: a leaf appearing twice CANCELS. Measured --
- **`reduce_sum_exact_partitioned`** -- Sum a list of BUCKETS of contributions, bit-identically under ANY bucketing -- 4-way, 7-way, or one
- **`refine`** -- Produce a result, have a CRITIC (any callable -- a metric, opponent agreement, a model, a human) score it,
- **`refine_where_uncertain`** -- COARSE-FIRST: run the cheap method everywhere, then pay for the expensive one ONLY where a per-cell
- **`reflect_transform`** -- A secondary (bounce) ray as a TRANSFORM of its parent: origin -> hit point, direction -> reflected about the
- **`refract`** -- Snell's-law refraction of rays at a surface (total-internal-reflection falls back to reflection).
- **`refresh_renderer`** -- The reproject-and-refresh loop as a render mode: warp the previous frame forward and shade only the
- **`refresh_report`** -- Run the refresh loop and report {shaded_fraction, psnr_mean, psnr_worst, psnr_first, psnr_last,
- **`regime_detector`** -- A DOUBLE-DIFFUSIVE regime/layer detector, borrowed from ocean physics: a FAST component tracks the
- **`regime_gate`** -- Build a REGIME GATE (holographic_regimegate) -- route to a superior-but-NICHE method only when a cheap
- **`registered_commands`** -- The allowlist: {name: doc}. The ONLY external programs run_command can run. See
- **`reinforce`** -- Reinforcement-learning update: teach the decision brain that taking `action` in `state` earned `reward`.
- **`replan_needed`** -- The cheap per-tick guard for a baked Plan: should the courier abandon it and re-anchor (call
- **`reproject`** -- Predict frame `b` from frame `a` by warping it with the recovered motion. `tile=None` uses one global
- **`reproject_report`** -- The comparison carried WITH the capability: {no_warp, global, tiled, uniformity, best}. `no_warp` is the
- **`reservoir`** -- Gradient-free SEQUENCE learning -- the substrate-native Echo-State Network, the truly
- **`residue_system`** -- Exact integer arithmetic in vectors via a RESIDUE NUMBER SYSTEM (holographic_extras) -- encode integers
- **`resolution_profile`** -- How much holographic RESOLUTION does classifying this input need? For
- **`respond`** -- Query-and-generate: answer a query with a continuation steered toward
- **`respond_report`** -- Answer a query AND measure the answer: returns the response with its
- **`restore`** -- Restore a degraded measurement y = A(clean) + noise by Plug-and-Play / RED -- the inverse-problem
- **`restore_procedure`** -- Restore a degraded measurement y = forward(clean) + noise by running Plug-and-Play/RED AS A VSA PROGRAM
- **`reversibility_audit`** -- Classify each base instruction as reversible (bind/unbind/permute/involution) or information-
- **`revoke`** -- Stop sharing: revoke a principal's READ access to a namespace (or list). See holographic_access.
- **`rigid_body`** -- A hardbody via shape matching (polar decomposition): falls and rotates under forces but never
- **`rmtf`** -- The rotation-measure transfer function (the 'dirty beam' in Faraday space) for a given wavelength^2
- **`robust_accumulate`** -- Average noisy estimates of one quantity robustly, for the engine's averaging paths (consolidation over
- **`room_acoustics`** -- GEOMETRIC ROOM ACOUSTICS: how a room echoes. `.rt60()` is the reverberation time (Sabine), `.reflections
- **`rope`** -- A hanging rope softbody of n particles (particle 0 pinned).
- **`sampler`** -- Modeling-app backlog (capstone): a placeable read-probe -- the read-dual of a FieldEffect. Reads a
- **`scale_is_not_a_bind`** -- MEASURE, do not assume, that a dilation is not diagonal in the Fourier basis: fit its 'spectrum' on one
- **`scale_node`** -- A cosmic-scale summariser over a scene hierarchy: a parent carries the monoid-accumulated value of its
- **`schedule_program`** -- Fill 4 (the scheduler capstone): run a VSA program DAG (built with holographic_schedule.{leaf,op_bind,
- **`scheduler_capacity`** -- Forecasting sweep (sec.5.5): the scheduler's cost model IS a forecaster. Instead of assuming the
- **`screen_ray`** -- Build a world-space RAY from a normalized screen coordinate (holographic_raypick) -- (screen_u, screen_v)
- **`sculpt`** -- SCULPT a field with a falloff-weighted brush (holographic_sculpt, FS-1) -- a local edit of a field
- **`sdf2d`** -- Build a 2-D SDF primitive by name (W10): 'circle' (r), 'box' (bx,by), 'rounded_box' (bx,by,r), 'ngon'
- **`seam_continuity`** -- How seamlessly a field tiles along `axis` (~1.0 = seamless, >>1 = visible seam). See
- **`segment_image`** -- DEMUX a photo into per-object REGIONS by colour (+ weak spatial coherence) -- the segmentation front end
- **`selection`** -- Modeling-app feature layer: a Selection helper bound to a Scene -- query objects into a set of handles,
- **`self_affinity`** -- Hurst exponent of a 1-D series read by the mind: 0.5 random walk,
- **`selftest_coverage`** -- Which engine modules carry a real selftest, and which don't -- the engine's own test-coverage census,
- **`semantic_scene`** -- Wrap an EXISTING list of scene objects ({shape,color,material,size,...}) as a SemanticScene so you can
- **`semidirect_law`** -- `max |A T(t) A^-1 - T(A t)|` -- zero, because the ideal is NORMAL. **One line, three costumes**: it is
- **`settle_island`** -- SLEEP IS THE CLOSED FORM: send an island straight to the fixed point of `x <- bind(U, x)` in ONE
- **`shade_adjoint`** -- The ADJOINT move done correctly for ANY affine: shade(A x, L) == max(0, n . (A^-1 L) / ||A^-T n||).
- **`shader_combine`** -- H7 -- blend M compiled shader variants into ONE transfer, exactly. An LOD stack, a multi-scale filter, an
- **`shader_pipeline`** -- H1 -- a filter GRAPH compiled to ONE transfer before any data is touched. Every stage (blur, translate,
- **`share`** -- Freeze this trained mind and return a SharedMind that many lightweight
- **`shared_definition`** -- A shared, editable scene DEFINITION -- geometry bound to a material, with the binding TYPE-CHECKED at
- **`sharpen_image`** -- IMAGE MANIPULATION: deblur / sharpen a signal or image by iterating a deconvolution loop toward the
- **`sharpen_loop`** -- Recover detail an over-smoothed signal LOST, by looping a converging negative-lobe (Van Cittert)
- **`sharpness_depth`** -- DEPTH-OF-FIELD DEPTH from a single image via LOCAL SHARPNESS (in-focus foreground = near, blurred
- **`should_jump`** -- Does jumping k substeps of a dim-dimensional island beat stepping them? True when k >= 20*dim --
- **`shrinkwrap`** -- SHRINKWRAP: move each vertex of `mesh` onto its closest point on `target_mesh` (Blender shrinkwrap /
- **`signed_distance_field`** -- Occupancy/inside mask -> signed distance field (negative inside, positive outside) via the fast-sweeping
- **`signed_distance_field_3d`** -- Occupancy VOLUME -> signed distance field via 3-D fast-sweeping eikonal (Numba ~230x on 96^3, bit-exact
- **`similarity_graph`** -- A geometry-weighted kNN graph over hypervectors (holographic_simgraph, ARCH-3): the cotangent-Laplacian
- **`simulate_hair`** -- HAIR DYNAMICS (H2): simulate strands as PBD chains (root pinned, inextensible with Follow-The-Leader,
- **`simulate_snow`** -- Physics backlog (#8B): seed a snow block and run it -- it falls, piles, and compresses plastically.
- **`simulation`** -- The SIMULATION SCAFFOLD (R9): wrap ANY time-stepped solver in ONE step loop, without flattening the
- **`singularity_index`** -- The per-vertex singularity index, EXACTLY a multiple of 1/4. sum(index) == the Euler characteristic --
- **`sketch2d`** -- 2D CONSTRAINT SKETCH (K8): a parametric sketch solved by iterated projection. Add points, declare
- **`skills`** -- The full machine-readable SKILL MANIFEST: every curated capability home plus every public method (with its
- **`slime_solve_maze`** -- SIMULATION: solve a maze with a colony of slime-mold walkers laying pheromone into ONE holographic
- **`snapper`** -- Modeling-app feature layer: a Snapper that snaps a dragged point to the nearest grid node or vertex
- **`snow_mpm`** -- Physics backlog (#8B, rung 4): a SNOW solver by the Material Point Method (Stomakhin 2013). Seed it
- **`softbody_sleep_tracker`** -- A SleepTracker sized for a SoftBody's islands. Pass it as `body.step(..., sleep=tracker)`: islands at
- **`solidify_mesh`** -- Give a surface thickness (the shell / solidify modifier): offset an inner copy along the vertex
- **`sparse_field`** -- Build a NARROW-BAND SPARSE field from a dense field function (holographic_sparsefield, FS-2) -- store,
- **`sparse_reconstruct`** -- The reusable sparse-probe-interpolate-refine pattern: reconstruct a known deterministic field (oracle) over
- **`spatial_hash_pairs`** -- Find all index pairs within `radius` via a uniform-grid cull -- O(N) expected vs O(N^2), the
- **`spatial_index`** -- Sweep 3 item 2: ONE shared uniform-grid spatial index over a point set -- radius / knn / closest-point
- **`spectrum_cache`** -- Fill 1 (residency): a content-addressed cache of atom -> rfft(atom), so binds/unbinds against KNOWN
- **`spectrum_to_rgb`** -- What the HUMAN eye sees from a spectrum, as sRGB -- the general spectrum->colour door (blackbody_rgb
- **`sphere_mask`** -- A spherical solid-obstacle mask (1 inside the ball) for fluid_step_3d/smoke_step_3d `solid` -- the
- **`sphere_trace_trapped`** -- Sphere-trace rays AND return each ray's ORBIT TRAP -- the closest approach of its march to a trap set
- **`star_cluster`** -- Place `n` star systems in a field -- the UP direction of star_system (a cluster is many systems). Masses
- **`star_system`** -- PLUG DATA IN, GET A STAR SYSTEM: assemble physical parameters (star temp/radius/mass; planets a/e/radius/
- **`steady_heat`** -- STEADY-STATE heat on any SDF shape: hold the boundary at `boundary_temperature(point)` and find the
- **`steering_regress`** -- Anisotropic (steering) kernel regression (RT-IV1): fit a PER-AXIS bandwidth to the data's directional
- **`step_at`** -- What is the i-th step of a stored plan?
- **`step_islands`** -- Advance ONLY the awake islands one frame: (new_state, awake_ids, asleep_ids). A sleeping island's rows
- **`storage_array`** -- A federated, RAID-style symbol store -- the capacity/resilience faculty from the Path D
- **`storage_spine`** -- Sweep 3 item 7: one content-addressed, deduplicated, erasure-robust byte store -- uri KEYS a record,
- **`straight_line_cells`** -- The grid cells a straight line start->goal crosses -- the tie-break-independent baseline a naive shot pays.
- **`strain_directions`** -- Per-face PRINCIPAL STRETCH direction of a deformation (rest mesh -> deformed_vertices) -- the DEFORMATION
- **`structure_compresses`** -- The link itself: correlation between window structure scores and their
- **`structure_score`** -- Does this stream have REUSABLE STRUCTURE? The mean chunk depth of its own learned tokenization: 1.0 =
- **`structured_index`** -- A content-addressable structured index over a list of keys (holographic_tree.StructuredIndex)
- **`subdivide_sequence`** -- Subdivide a SEQUENCE of hypervectors into a smooth limit curve (holographic_subdivcurve, ARCH-5):
- **`subsurface`** -- Field-native subsurface translucency: measure how much SOLID the light crosses inside the object to
- **`superellipsoid`** -- A SUPERELLIPSOID surface as a point grid: `e1,e2` squareness (1,1)=ellipsoid, ->0=box, >1=star; `a,b,c`
- **`superpose_batch`** -- Fill 3 (auto-superposition + spill): pack N independent keyed items into the FEWEST superposed vectors
- **`superpose_compute`** -- The WIDTH faculty: evaluate K computations at once inside ONE vector (Kanerva / Kleyko 'computing in
- **`surrogate_zscore`** -- Measure a structure `statistic(x)` against an ensemble of PHASE-RANDOMIZED surrogates and report how
- **`svg_canvas`** -- The holographic vector-graphics (SVG) faculty (holographic_svg.HolographicSVG) -- the sharp,
- **`svgf_denoise`** -- Interactive Render Speed (technique E): edge-aware denoise a noisy (1-spp-style) image the engine's
- **`sweep_directions`** -- The UP / DOWN / SIDEWAYS completeness sweep (holographic_ladder): does the ladder's structure-finding
- **`sweep_tube`** -- Sweep a 2-D `profile` (default a circle of `radius`) along a 3-D curve into a watertight TUBE mesh,
- **`symbolic_gradient`** -- Exact gradient of a symbolic scalar field as a pure-NumPy function (force = -symbolic_gradient(energy)).
- **`tearable_cloth`** -- A TEARABLE thin sheet: a PBD cloth whose links SNAP when stretched past the material's tear strength,
- **`temperature_to_biome`** -- Map a planet's equilibrium temperature (K) to a surface regime (frozen/cold/temperate/hot/molten) -- the
- **`template_names`** -- The names of the available parameterized recipe templates (ISA-6, the macro layer).
- **`temporal_reuse`** -- Sweep 3 item 5: the temporal-reuse loop -- reuse last frame's per-cell result, reproject it
- **`tensor_bind`** -- A TENSOR-PRODUCT (outer-product) binding memory -- the uncompressed cousin of HRR's circular
- **`tensor_structure`** -- Will a tensor factorisation pay for this array -- BEFORE you pay to find out?
- **`terrain`** -- G4 -- a holographic fBm heightfield, liftable to a displaced-grid mesh or a heightfield SDF.
- **`tile_field`** -- Tile an FPE field hypervector over an n-D lattice -- domain repetition as bind+bundle, so the result
- **`tile_field_recursive`** -- Recursive tiling (inception): tile the tiling `levels` deep -- count^levels copies per axis from
- **`time_of_impact`** -- CONTINUOUS COLLISION DETECTION by conservative advancement: (hit, toi, contact) for points X moving at V
- **`timeline`** -- A keyframe Timeline (holographic_anim): `.key(channel, t, value, interp='linear')` then
- **`to_shadertoy`** -- Emit a complete, runnable SHADERTOY fragment shader for an SDF (holographic_sdf.sdf_shader) -- map() +
- **`to_state`** -- Snapshot the learned mind for holographic_core.save (so quant='rd'/'auto'/'int8' all apply).
- **`topic_pull_tradeoff`** -- The honest experiment surfaced on the brain: for each topic_weight,
- **`torus_knot`** -- A (p, q) TORUS KNOT curve: winds p times around the axis, q through the hole (p=2,q=3 = trefoil).
- **`trace`** -- The full provenance answer: STYLE (transition bag) AND MATERIAL
- **`trace_streamlines`** -- Trace STREAMLINES (integral curves) of a per-face direction field across a triangle mesh -- walk along the
- **`train_navigator`** -- Train a learned navigator over `items` ((N, D) vectors) and hold it on this mind. Returns the world's
- **`transfer_uv`** -- TRANSFER per-vertex UVs (or ANY per-vertex attribute) from source_mesh onto new target_vertices by
- **`transform_bank`** -- A prebuilt map of named hypervector transforms, held as their Fourier spectra. `add_random_unitary`,
- **`transform_selection`** -- The GIZMO BACKEND (holographic_transform_space): transform selected vertices about a PIVOT
- **`translate_kernel`** -- Translate a kernel between languages -- python | c_f64 | c_f32 | wgsl | js | zig_f64 | zig_f32 --
- **`translate_rule`** -- H2 -- slide an entire compiled gather rule by `dx`, for ONE bind, at a cost independent of N. The encoder
- **`transport`** -- An animation TRANSPORT / playhead (holographic_anim) -- start/pause/step/seek/scrub/rewind/fast-forward
- **`tree_structure`** -- Encode an expression tree as a typed structure at this mind's dim/seed. A leaf is a str symbol;
- **`trefoil_knot`** -- The TREFOIL knot (simplest non-trivial knot) as (n, 3) points. See holographic_curves.trefoil.
- **`triage_code`** -- Triage code in an UNRECOGNIZED language (C5): honest structural OBSERVATIONS -- ranked identifier word
- **`trim_loop_from_curve`** -- Project a 3-D trimming curve (e.g. a K2 SSI polyline) to a (u,v) trim loop -- the K2->K3 bridge. See
- **`trimmed_surface`** -- TRIMMED SURFACE (K3): a surface surf_uv(u,v)->(x,y,z) restricted to trim loops in parameter space (inside
- **`turnaround`** -- TURNAROUND: render a mesh from the standard modelling views (top/front/side/3q) in one call and, if a
- **`typed_structure`** -- A fresh StructureRecipe bound to this mind's dim and seed -- the single replayable build-graph
- **`unbind_keys`** -- Unbind one trace against many keys in ONE batched FFT (unbind_all) -- the vectorised form of the
- **`uncertainty_concentration`** -- The coarse-first GATE, and it costs nothing: how concentrated the uncertainty is (0..1), i.e. the share
- **`unpack_images`** -- Reconstruct the image family packed by mind.pack_images -- bit-exact, byte for byte.
- **`unproject_depth`** -- Turn a depth map into 3D points in camera space (the pinhole unprojection). Returns (H, W, 3). See
- **`upscale`** -- Inverse-rendering IR12: FSR1-style spatial upscale -- EASU (edge-adaptive Lanczos with anti-ringing) then
- **`use_gpu`** -- User setting: turn the optional CuPy GPU backend on/off for the heavy array-parallel kernels (fluid
- **`use_holographic_brain`** -- Swap the (already-declared) creature brain to a holographic backend in place -- 'routed' for the
- **`vanishing_point`** -- Estimate the dominant VANISHING POINT of a scene's linear perspective from its strong OBLIQUE Hough
- **`vector_function_encoder`** -- An N-dimensional Fractional Power Encoder (holographic_fpe) on this mind's dim and seed: encode a
- **`vegetated_terrain`** -- S2 -- a fBm terrain (G4) with L-system plants (G5) scattered on its surface -> one scenegraph.
- **`versioned_store`** -- VERSIONED STORE with rollback (holographic_history, VersionedStore) -- a store whose every version is
- **`vertex_move_command`** -- A reversible VERTEX MOVE command (holographic_edithistory) for the undo log -- apply adds `delta` to the
- **`video_codec`** -- MOTION-COMPENSATED VIDEO/FRAME CODEC (holographic_video, HolographicVideo) -- the rigid-shift-is-a-bind
- **`volatility_structure`** -- Does a return series carry the volatility-clustering signature of real
- **`vorticity_confinement`** -- Vorticity confinement force (Fedkiw 2001) -- restores the small vortices semi-Lagrangian advection
- **`voxel_centres`** -- World-space centres (m, 3) of the SOLID voxels of an occupancy grid -- a point cloud of the volume,
- **`voxel_remesh`** -- VOXEL REMESH (Blender Voxel Remesh): rebuild a mesh as a UNIFORM, watertight surface by sampling it into
- **`voxelize_mesh`** -- VOXELISE a triangle mesh into an occupancy grid via the generalised WINDING NUMBER (Jacobson 2013) --
- **`voxelize_sdf`** -- VOXELISE an SDF / field into an occupancy grid: solid where field <= iso. O(voxels), no winding number
- **`vsa_region`** -- A REGION of space as a signed-distance ball with boolean algebra (holographic_extras) -- union /
- **`warp_gather`** -- IMAGE/FIELD WARP: resample `values` (defined at `positions`) at `query_source_positions` by BACKWARD
- **`warped_noise`** -- DOMAIN-WARPED fBm (W11, iq's warped noise / dFBM): fbm sampled at a point displaced by a vector of
- **`wasserstein`** -- The Wasserstein (earth-mover's) distance between two distributions by Sinkhorn iteration
- **`wave_field`** -- A scalar acoustic PRESSURE field that PROPAGATES (the compressible wave the incompressible fluid can't
- **`wave_packets`** -- Physics backlog (N8): a water surface as localized WAVE PACKETS -- each a Gaussian-enveloped wave train
- **`wavelength_to_rgb`** -- The approximate sRGB a human sees for a monochromatic light at wavelength `nm` (UV/IR map to black --
- **`weld_mesh`** -- Merge-by-distance: weld vertices within `tol` into one (mean position), remap faces, drop the faces
- **`winding_map`** -- When a carrier LARGELY reverses (rectify_carrier's monotone_fraction well
- **`word_taxonomy`** -- The 'what kind of thing is this?' chain from the vendored dictionary's taxonomy: e.g. 'dog' -> ['domestic
- **`workspace_manager`** -- A WORKSPACE MANAGER (holographic_workspace) over this mind's database -- durable user data coexisting
- **`worth_factoring`** -- Would factoring this field actually save bytes? {worth_factoring, factored_bytes, dense_bytes}.
- **`xyz_to_srgb`** -- Convert CIE XYZ readings (from the human observer) to sRGB, matching blackbody's exact conversion.
